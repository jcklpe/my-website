# Case Study Hero — To Do

## Background

Conceptual doc: `docs/case-hero.md`. This doc tracks the gated implementation.

Working direction (decided 2026-06-10): **editorial-split**. The case-study card is image-above / cream-text-plate-below; halftone is preserved as image-area styling. The active work is the Selected Work composition pass — an authored silhouette "score" so the card stack stops reading as a uniform slab. The detail-page hero still needs its own editorial-split reconciliation. See the conceptual doc's "Decision — editorial-split wins" and "The composition problem" sections.

## Project Organization

This spike is **gated**. Each phase ends at a decision checkpoint with human visual QA. We do not move to the next phase until the current one is confirmed visually right (or until we decide it's wrong and pivot). The "To Do" section below lists only the *active* phase plus its decision gate; later phases live under "Future Phases (Gated)" as scoped intent, not decided work.

Each gate is a real decision point. It can:

- **Proceed** to the next phase.
- **Iterate** within the current phase (adjust parameters, retry).
- **Pivot** to a reserve direction (specimen plate, split layout, etc.) if the current direction isn't landing.

## General Principles

- One piece at a time. Card composition score first, then detail-page reconciliation, then transition polish. Do not pre-decide downstream phases.
- Visual QA between every phase. Each phase produces something the user can see in a browser before we decide what's next.
- **BTAK rule**: no decorative labels that perform meaning ("CASE STUDY · 2024", date strips, etc.). Materials carry the meaning, not labels.
- The featured-media transition contract (`data-featured-*` attributes, slip-source geometry hooks) must remain intact even if the visible slip is removed — the transition code reads geometry from these hooks. The title region needs to stay addressable.
- Composition variance is delivered with grid placement and real widths, not `transform: translate` — the FLIP transition reads `getBoundingClientRect` truth from the geometry hooks. The text plate stays a single clean rectangle.
- The composition score is phrased, not alternated — no strict L/R ping-pong — and must degrade gracefully from 3 to ~8 case studies (launch reality is ~4).
- CSS-only halftone (leanrada's approach). Do not reach for SVG/WebGL/canvas — validated as unnecessary, and CSS is friendlier for animation, hover, and perf.
- Use existing FLIP infrastructure. Do not add GSAP.

## Current State Overview

As of 2026-06-10 (note: the card/section editorial-split changes are in the working tree, not yet committed):

- `CaseStudyCard.vue` is an editorial-split: grid with the halftoned image area in the `1fr` top row and the cream text plate (`.link-box`) as the auto bottom row. Ink text always. Zero-padded ordinal badge in signal-blue mono above the title. Hovering the plate drops the duotone filter and fine-grains the halftone (color-reveal scoped to the click target). The earlier layout variants (floating / specimen-plate / bleed-band) and the cream-title toggle were stripped when the decision landed.
- `HomeSelectedWorkSection.vue` provides per-index halftone seed presets (`SEED_PRESETS`, six variants cycling) to cards via provide/inject, plus the SVG duotone filter defs. Its live controls panel is gone; the seeds carry the variance. This provide() pattern is the natural delivery mechanism for the upcoming layout score.
- `pages/case-studies/[slug].vue` still renders title-on-halftoned-image (no slip panel, no split) and still carries the spike controls panel (the `<details>` duotone/legibility controls). Not yet reconciled with the editorial-split decision.
- The slip mixins in `_featured-media-overlay.scss` are no longer consumed by the card; remaining consumers are `CaseStudyLoopNav.vue`, `FeaturedMediaTransitionLayer.vue`, and `pages/writing/[slug].vue` (the writing consumer belongs to the bento-writing spike).
- The featured-media transition (`composables/useFeaturedMediaTransition.ts`, `components/transitions/FeaturedMediaTransitionLayer.vue`) reads geometry from `data-featured-*` hooks on cards and details. Working and not to be regressed.
- Case-study count reality: ~4 at launch (several inherited decade-old studies will likely be removed); the composition must flex from 3 to ~8.

## Status — editorial-split decided; composition pass active (2026-06-10)

The (A)-vs-(B) decision is made: **(A) editorial-split won.** The card carries the split (working tree, uncommitted). Direction (B)'s figure-ground-inversion findings stay documented in the conceptual doc, and its variant settings remain candidates for image-area styling only — legibility no longer depends on them. The halftone exploration history, settings matrix, and durable lessons all live in the conceptual doc (`docs/case-hero.md`).

The new active problem, surfaced immediately after the split landed: the Selected Work section reads as a **uniform repeating slab**. Every card has the same full-width silhouette, so halftone variants and the ordinal badge don't register at scanning distance. A brainstorm pass (2026-06-10, the henry reference site as the named reference) produced a ranked set of variance axes, a BTAK discard pile, and a phased plan; the conceptual doc's "The composition problem" section is the full writeup.

Plan shape: **silhouette score first** (authored card width/anchor/height per index), margin-ordinal as fast-follow, plate-anchor drift and interstitial bands held for a second pass only if the silhouette score alone doesn't break the slab read.

## To Do — Selected Work silhouette score (active)

Axis 1 of the composition brainstorm: vary card silhouette (width + horizontal anchor + height) via an authored per-index score. Plates stay full-width-of-card this pass; no plate-anchor drift, no interstitials yet.

Band-preserving revision (v2) landed 2026-06-10 (see Ready for Human QA). Remaining in this phase:

- [ ] Tune the score from visual QA feedback. Knobs: plate stops, height clamps, and `MIDDLE_PHRASE` order in `CaseStudyList.vue`; plate width/border/shadow in `CaseStudyCard.vue`; `row-gap` in the list.

### Decision gate — silhouette score visual QA

- Does the Selected Work section read as a composed path rather than a repeating slab, at normal scroll speed?
- Does it read as horizontal strata — clearly distinct from the bento grammar reserved for Latest Writing?
- Does the negative space (cream beside the docked plates) look authored, not like a broken layout?
- Does the transition still read clean from plates at different stops?

Outcomes: **proceed** to the counterweight ordinal → then decide whether interstitials (or a single accent inset) are still wanted; **iterate** on the score phrasing or plate materials; or **pivot** back to the brainstorm's axis list if the band score reads gimmicky.

## Queued — after the composition gate

Held in order, not started:

- [ ] **Counterweight ordinal.** The ordinal scales up and holds the *opposite* end of the full-width text plate from the docked text block — number one end, text block the other, the two-element tension that gives the reference archive's rows their horizontal energy. Same true ordinal, no new chrome. Decide after QA judges whether the empty plate-stretch wants it.
- [x] **Detail-page reconciliation — Phase A landed 2026-06-12** (carved hero; see Ready for Human QA). Phases B/C below.
- [x] **Halftone default → 6px** (2026-06-13): 4px was moiréing on some images; 6px is the committed hero default.
- [x] **Transition corner pop fixed** (2026-06-13): the real complaint was the media corner *popping* square→rounded instead of morphing. The clone now animates `border-radius` from the source corner (card = sharp `0px`) to the target corner (hero = `0px 0px 900px`, captured by walking up from the tracked media frame to its rounded clipping ancestor `.hero-plate` via `borderRadiusFromElement`). The rectangular media clip-path was dropped on the clone (it would have squared off the rounding; `overflow: hidden` + the animated radius do the clipping). Halftone stays on the clone per the user (removing it looked worse, popping no-dots↔dots). Verified the radius capture returns `0px 0px 900px` from `.hero-plate`.
  - **Root fix (2026-06-13): the corner morph was weird because the tracked media box ≠ the rounded plate.** The layered hero's `.hero-halftone-box` was `position: relative` with auto height, so it collapsed to the image's aspect height (~774px) while `.hero-plate` was the full `--hero-plate-height` (1000px). The featured-media transition tracks the media frame, so the clone flew to the 774px box while the 900px corner radius lived on the 1000px plate — wrong-sized clone, over-rounded corner, bottom mismatch on landing. Fix: layered halftone box is now `position: absolute; inset: 0` with the pane/media at `height: 100%` (mirroring the cards' image area), so the image fills the plate and media-box == plate (both 1175×1000). Verified mid-flight at a 12s slow-mo: clone box matches the plate and `border-radius` animates `0px` → `0px 0px ~900px` (morphs, doesn't pop). Static hero unchanged visually (the old ink gap below the 774px image was already hidden under the rising body). Halftone stays on the clone. `corepack pnpm check` passes.
  - **Perf note (pinned, not addressed per the user):** the halftone on the resizing clone re-rasterizes at the size extremes (~450/283ms bracket frames measured earlier). Left as-is; only revisit if it reads janky in use.
- [x] **Transition polish pass (2026-06-13)** — five reported issues:
  1. *Body covered by the photoplate while tweening in* + 5. *dark halftone "destination plate" visible during flight*: both were the destination hero composite (ink bg + dot pseudos + K-layer) showing during the flight — `FeaturedMediaFrame` only hid the `<img>`. Fix: the whole `.hero-plate` now takes `is-transition-hidden` (opacity 0) while transitioning, so nothing of the destination shows under the clone and it can't cover the rising body.
  2. *Flying title plate semi-transparent*: the clone's `.slip-bg` used `slip-surface` (93% transparent). Overridden to opaque `var(--color-surface)`, border none.
  3. *Card number/text popped out on click*: the card's `.plate-content` (number + title + excerpt) now carries the hide class with an `opacity 200ms` transition, so it fades.
  4. *Reverse transition popped colour→duotone on landing*: hand-off cross-fade — at the end of the motion `active` flips false (un-hides the real destination, since hide conditions key off `active`) and the clone fades out over it via a plain **Vue `<Transition name="media-handoff">` leave** (240ms opacity), media kept until the leave ends. Reverse = colour clone dissolving into the duotone card; forward = colour→colour (invisible).
  - **Correction (2026-06-13, second pass):** the first attempt used an orchestrated JS `fading` phase that un-hid everything mid-transition — it caused a full-screen "respawn/re-tween" blip and didn't actually fix issues 1/3/4 (user feedback). Reverted the phase machine entirely; replaced with the plain Vue leave-transition above (issue 4) and these:
    - **Issue 1** (body tweening under the plate): hit-tests confirmed the body is z-above the plate at rest *and* mid-rise; the perceived under-plate was the blip. The plate-hide (issue 5) covers the transition window. No z-index change needed.
    - **Issue 3** (number/text popped instead of fading): the home page unmounts ~1 frame after click, cutting the card's 200ms fade. Added a `SOURCE_FADE_HOLD` (200ms, reduced-motion-aware) before `navigateTo` so the source fade plays; the clone is already up at the source during the beat. Verified the card's `.plate-content` opacity ramps `1 → 0.78 → 0.29 → 0.13 → 0.02`.
    - Browser-verified all: forward lands clean (body over plate), card text fades, reverse clone leave-fades `0.23 → 0.03 → gone` over the un-hidden duotone card, no blip. `corepack pnpm check` passes.
- [x] **Body-under-photoplate, real cause + fix (2026-06-13)** — the body wasn't under the *real* plate (z-index fine; hit-tested correct at rest). It was under the **teleported media clone (z 900)** during the transition, and the body's `detail-content-rise` entrance (translateY 46vh) made it visibly *tween* under the clone. The same animation also played on a plain page refresh, which shouldn't animate at all. Diagnosis matched a parallel report from the bento-writing agent. Fix (low-risk, no overlay restructure): **removed `detail-content-rise` entirely** from both `case-studies/[slug].vue` and `writing/[slug].vue` — the body now sits in place and is revealed as the clone hands off. Verified: refresh loads static (`animation-name: none`), forward transition body settles over the plate with no tween, both pages. `--motion-route-content-delay` token is now unused (left defined, harmless).
  - **Overlay split done (2026-06-13).** Removing the content-rise stopped the refresh-animation but the body still only appeared *after* the transition — it was still under the single z-900 clone during the flight. Verified there's **no stacking context** between `.content` (z2) and `<body>` (`.case-study-page`/`main`/`.site-shell`/`#__nuxt` are all static), so the split the writing agent proposed is safe here. `FeaturedMediaTransitionLayer` now renders **two teleported layers**: `.ftml-layer--media` (the halftone plate clone) at z-index 1, and `.ftml-layer--text` (slip/title/meta) at z-index 901 — the page article content (z2) sits between them. The body now renders over the flying photo *during* the transition, title band over the body. Shared layer ⇒ fixes case-study and writing posts together. Browser-verified: mid-flight the body+title composite over the halftone; forward/reverse complete with both clone layers removed, no overflow. `corepack pnpm check` passes.
  - **Reverse rework — root cause was a NuxtLink race (2026-06-16).** Forward (home → detail) was perfect; reverse had: body popping out instantly, an empty duotone card-plate the photo slid under, and a "fly to ~75% then pop into the card." Root cause for the timing problems: the reverse links (SiteNav Home + writing items, SiteFooter selected-work) were **plain `<NuxtLink>`** — NuxtLink fires its own `router.push` the instant it's clicked, racing and defeating our handler's `event.preventDefault()` (which only stops the *native* anchor nav, not NuxtLink's programmatic push). So the page navigated at ~0.25s regardless and the transition timing was never ours to control. The forward works precisely because the detail **cards use NuxtLink `custom` mode** (render their own `<a>`). Fix: converted all reverse links to `custom` mode so our handlers own the timing (handlers now also handle the plain-SPA-nav and modified-click cases). With that in place:
    - **(b) Body down+out exit.** The detail body now plays the mirror of the arrival rise — `detail-content-exit` (translateY 0 → 46vh + opacity 1 → 0) — driven by an `is-leaving` flag (a watcher on `transitionState.active` catches the false→true *departure*, since arrival has it true at mount). The composable's new `waitForBodyExit()` holds for the route duration before navigating so the body fully leaves the still-mounted detail page (it only exists there). Verified: body translateY 274→447 / opacity 0.39→0.00 over the hold, url stays on the detail page throughout.
    - **(d) Two-step hand-off.** Clone flies/morphs *fully* into the card geometry (card photoplate kept hidden, opacity 0, the whole flight — `TRANSITION_TARGET_READY_TIMEOUT` bumped 900→2500ms so the real card is reliably found instead of falling back to a stale cached rect → no more "75% pop"), and *then* the duotone crossfades in: `.card-image-area` gets `transition: opacity 240ms` so removing `is-media-transition-hidden` at hand-off fades the real plate IN while the clone fades OUT, both co-located (240ms matches the clone's media-handoff leave). Verified trace: mediaTop eases 114→299 to its seat, cardOp holds 0 the whole flight, then 0→1 only once the clone is seated.
    - Forward still rises cleanly (`is-arriving`, `leaving` stays false); refresh stays static; both directions land clean (clones removed, cardOp 1, no overflow); no console errors. `corepack pnpm check` passes.
  - **Deferred:** (1) the hand-off cross-fade still uses a Vue `<Transition>` leave on the clone; user flags Vue/native transitions as a possible dead-end vs the custom geometry system — revisit if polish needs it. (2) **Stretch (c) not done:** surrounding home elements (other cards, headings, BLUF) should assemble out on forward / in on reverse so nothing pops — deferred by user choice. (3) Body exit + flight are currently *sequential* (body falls, then photo flies) rather than concurrent — a consequence of the body living only on the source page; acceptable for now, revisit if it should overlap.
- [ ] **Detail hero Phase B — the morphing arrival.** FLIP clone animates border-radius 0 → the carve as it lands; duotone → full color; coarse → final screen; the slip clone dissolves plate-into-ground (border/background fade as it becomes the shelf). Gate: does arrival feel like *resolving*, and does the reverse (detail → card) re-objectify the plate cleanly — the reverse direction is the one most likely to feel wrong.
- [ ] **Detail hero Phase C — transition ghosts, both modes, behind a control.** Full-treatment trail (straight Codrops read) vs. dot-field misregistration ghosts (print-native read; echoes at coarse screen resolving into the final plate). Build first, judge visually, profile after — no pre-optimization.
- [ ] **Konami retirement for the spike controls.** At spike close the hero/duotone controls panel goes behind a Konami-code keydown listener as a shipped easter egg instead of being deleted. Client-only, hidden by default.
- [ ] **Narrow the halftone variant set.** Homepage side executed in v6.3 (direct duotone only; ink-blue bookends, blue-cream middles). Remaining: the detail-page hero still cycles spike settings via its controls panel — commit its treatment during detail-page reconciliation, and decide whether unused modes (crisp/bleed/tritone) and their SVG defs are deleted or kept as documented options.
- [x] **CMS authorability — executed** (v7.2 composition radios, v7.4 photo-treatment radio). All three per-case-study controls live in the "Selected Work Display" ACF group: row layout, text-plate alignment, photo treatment; `auto` defers to the page score/cycle in every case. Open judgment left as-is: the score phrase needs no CMS-side ordering control — order comes from publish order, layout from the radios.

### Spike-close gate (after the queue lands)

- Does the case-study surface (card + detail) read as Blue Atlas — structural / specimen / diagram register — not as a stylized image stack?
- Is title legibility unconditional (text on solid ground everywhere)?
- Does the card-to-detail transition work end-to-end from every layout preset?
- Is the spike narrative complete enough to archive: conceptual doc + this to-do moved to `docs/archive/`, durable lessons folded into `docs/visual-design.md`, halftone mixin library marked image-area-only?

## Likely future work — held loosely

Direction sketches, not committed plans. Listed so they don't get lost; expect them to evolve or get dropped as we learn from the current work. Don't let this list pre-frame the next step's design.

- **Plate-anchor drift.** Caption-plate at ~5 horizontal stops (L / CL / C / CR / R) under full-width cards — the second melody line. Only if the silhouette score + margin ordinal leave the rhythm wanting.
- **Interstitial coarse-halftone bands.** ~~Held.~~ **Promoted and built 2026-06-10** at the user's request under the naturalist's-field-book register reading (see the conceptual doc). Now in Ready for Human QA.
- **Atlas numerals (text home + giant drifting ordinal).** Captions go back to a consistent left anchor on every band; the oversized signal-blue ordinal becomes the expressive element that drifts/scales (possibly cropping off the band edge, aria-hidden, true ordinal stays in the text block). Proposed 2026-06-10 as the more document-native variance carrier; user is **willing to see a try but not bought in**. On deck after the strip QA verdict.
- **Zine-register editorial imagery.** Ghost-title wallpaper and stronger zine moves are not categorically out — the user will produce bespoke editorial visuals where they serve the voice, with the standing caveat that neural-net-generated work must not read as generic AI art. Open register question, parked in the conceptual doc's "Held, not discarded" section.
- **Frame-breaking gesture (rounded corner)** — still in play once the composition is settled.
- **Cleanup and archive** — when settled: coordinate with the bento-writing spike before deleting shared mixin code (`_featured-media-overlay.scss` slip mixins still feed `CaseStudyLoopNav`, the transition layer, and the writing detail page); fold durable lessons into `docs/visual-design.md`; move spike docs to `docs/archive/`.

## Ready for Human QA

- **Detail hero Phase A.2 — the layered hero (2026-06-12, user's sketch).** Review verdict on A.1: not a fan of the carved shelf, the mount, or carving generally — the preferred prior experiment was "just a 500px bottom-right radius." The new direction took two sketch read-backs to land (lesson: I kept adding mechanism — concave carves, text wrap-along-arc — that the sketch never contained; the actual move is **layering**). Implemented as the `layered` layout, now the default: photo plate at the top anchored to one side (~80% width, knob), its outer bottom corner swept by **one giant circular radius** (500px default — the plate's own corner, a curve in, not a carve out); the title column is **plain page ground layered over the plate's lower inner region** — straight column, no border, no panel, ignores the curve entirely (the desert-jackalope page-over-plate move). The article's opening paragraphs (≤3) are pulled into the column via their rendered HTML (toggle: "Pull lede into the column") so the layered feel includes real copy; the remainder renders through BlockRenderer as usual. Carved/mount/mount-carved remain in the select for comparison. Browser-verified at 1440 + 375: correct anchoring/overlap, 500px radius, cream column, no hero-caused overflow; the `vue/no-v-html` page warning is inline-disabled with a note (graduates into a content component if the lede pull survives). `corepack pnpm check` passes.
  - **Check:** does the layered composition match the sketch's intent at last? Knobs: plate width ("Plate / mount width"), overlap depth ("Shelf / overlap height"), radius ("Carve sweep"), side, lede pull.
  - **Check (open question from the discussion):** does this layout feel like the *case-study* hero, or — per the journal-vs-exhibit instinct — does it actually belong to the writing detail page, with case studies keeping a simpler full-bleed band + corner radius? Both candidates are now one select away (`layered` vs. a full-width banner read of the old direction); decide after living with it.
  - **Check:** lede pull on vs. off — with it off, the column is title-only and shorter.
  - **Check:** phone — plate full-width and shorter, overlap capped at 24vh; verified clean.
  - **A.3 amendment (same day) — the fake paper top.** Title + body scootched up ~200px per review. Mechanism: raising the shelf shrinks the hero's flow height, so the body slab rises with the title; the slab's flat ground would behead the photo full-width (the user's translate experiment confirmed), so `.content` in layered mode gets a **shaped two-layer background** — the first `--paper-step` px (new "Paper step" knob, default 240) paint cream only across the tab (page edge → just past the article column's right edge, `calc(50% + var(--article-column)/2 + var(--space-5))`); the photo stays visible beyond it. Hard-stop gradients, not clip-path: paint is shaped, content is not. Below the photo the transparent zone sits over same-color page ground, so the step is invisible there and overshoot is harmless. Shelf default 240 → 440 (range to 560); mirrors for carve-left.
    - **Editorial precondition (durable if this lands):** blocks that rise into the band must be default-width — no wide/full alignments in a case study's opening blocks. User has accepted this constraint.
    - **Characteristic to judge:** studies whose body starts with a non-paragraph block (e.g. Deep Dimension VR opens with an `h2`) get no lede pull → title-only column → the same shelf value reads as much deeper photo loss. Levers: tune the shelf per-feel, start bodies with a paragraph in the CMS, or extend the lede pull to swallow headings too.
  - **A.6 amendment (2026-06-13) — refinements + experiment controls.** From review: (1) first body block (the "Case Study" h2) now gets a tight `space-5` (~24px) top margin in layered instead of inheriting the article heading-major spacing — `.content.has-paper-top > :deep(:first-child)`. (2) The cream column (both the title's header and the body's content tab) is widened by `space-5` each side so text keeps ≥24px of cream padding from the column edges. (3) New **"Title single line (no wrap)"** toggle — column mode wraps the title at the body-column width, single-line mode sizes the cream to the title and never wraps (`max-content`, capped at `100% - space-6`). (4) New **"Title position (L↔R)"** slider (`--title-shift`, 0–100%) — positions the title via the `left%/translateX(-%)` trick so it slides from flush-left (0, the look the user liked) through centered-on-the-body-column (50) to flush-right (100), at any width. (5) New **"Photo left bleed"** slider (`--photo-left-bleed`, 0–40%) — the right-anchored plate's width is `calc(100% - bleed)`, so 0% is a full-bleed left edge. Browser-verified at 1366: title-shift 0/50/100 all correct (50 aligns with the body column within 5px), single-line stays one line and fits, full bleed reaches x=0, h2 margin 24px, no overflow; phone still a clean stack. `corepack pnpm check` passes. Review caught two faults: (1) the content's cream covered the title's lower line — a stacking bug: the title (z-index 3) was trapped inside the hero's `z-index: 1` stacking context, which sits below the content (z-index 2) at page level. Fixed with `.is-hero-layered { z-index: auto }` so the hero stops forming a context — plate (auto) < content (2) < title (3) order holds. (2) The title bled hard-left (x≈32) while the body sat in the centered article column (x≈357), so the cream tab spanned x=0 across everything ("full left bleed"). Fixed by centering the title column on the same axis as the body's content track and making the cream tab a **centered column** (transparent → cream → transparent gradient bounded to `min(100% - space6, --article-column)`) instead of left-to-tab. Result: title + body + cream read as one centered column floating on the photo, halftone showing on both sides. Title/body left edges align within ~19px (mono vs sans `ch`). Browser-verified at 1366 + 375, no overflow, phone still a clean stack. `corepack pnpm check` passes.
  - **A.7 amendment (2026-06-13) — floats fixed + lede-pull removed.** Two review points. (1) *"Did you add a float guard?"* — No. Real cause: a bare `.alignleft`/`.alignright` image is a **direct child of `.content-flow` (display: grid), and grid items ignore `float`** — so floats only worked when an image got wrapped in a `FloatBreakoutGroup` (a flow container). The USCIS "Stop reinventing square wheels" image was followed by a `core/group`, which wasn't wrap-compatible, so it stayed a bare grid item and didn't float. Fixes in `BlockChildren.vue` (shared, affects all article pages — a correctness fix): added `core/group` to `floatBreakoutCompatibleBlocks`, and aligned lead images now **always** route through `FloatBreakoutGroup` (even with no following compatible block) so they're never bare grid items. Verified: the image floats left with the group's text wrapping beside it. (2) *"Why is there an excerpt under the h1?"* — that was the **lede-pull** (first ≤3 paragraphs rendered into the title column). Removed entirely (ref, control, `ledeHtml`/`bodyBlocks` computeds, `.lede` element + CSS); body renders straight from `caseStudyBlocks`. The first paragraph still rises into the band (the layered design), but it's the real body block now, not a pulled duplicate. Desktop float verified at 1366; `corepack pnpm check` passes.
    - Known/unchanged: phone shows the pre-existing nested-list body overflow (already spun off as its own task) — not caused by this change; floats correctly stack (`float: none`) on phone.
  - **A.9 — hero styling locked + Konami easter egg (2026-06-13).** The user dialed final hero values; baked into the `[slug].vue` ref defaults as the committed design: layered / carve right / sweep 900 / shelf 560 / paper-step 400 / shelf-width 56 / plate-width 77 / title-shift 7 / single-line on / photo-bleed 13 / hero-height 1000 / mode off (full CMYK) / ink+cream / halftone 4px. The control panel is no longer dev scaffolding — it's a **shipped easter egg**, hidden by default and toggled by the Konami code (↑↑↓↓←→←→ b a) via a client `keydown` listener (`devControlsVisible`). SVG duotone filter defs stay rendered so a duotone mode chosen in the panel resolves immediately. Browser-verified: defaults render with no panel, no overflow; Konami reveals and re-hides the panel. `corepack pnpm check` passes. Note: the carved/mount/mount-carved layout variants remain in the panel as part of the playground (not deleted).
  - **A.8 amendment (2026-06-13) — hero-height control + halftone-scale finding.** Added a "Hero height" spike slider (`--hero-plate-height`, 320–1200px, default 560) so the photo plate can run much deeper behind the rising body — viable now that floats carry the cream mat (body imagery over the photo stays legible). Verified at 1000px. Also a review finding across three real heroes (USCIS abstract render / Travis County building+sky / Job Corps people): **the halftone's success is image-dependent** — it flatters real photographic source (reads as risograph/screenprint, forgives dated/low-res material) and exposes clean synthetic gradients (USCIS render → moiré/noise, nothing for the dots to resolve into). Practical takeaways recorded for spike close: favor photographic hero images; a finer screen (~8px vs 11px) calms the abstract case and improves legibility on photographic ones too without losing the print read; per-case-study CMS photo-treatment control already allows running abstract heroes finer than photographic ones. Each hero takes its image's native colors (cyan/orange/blue), so heroes are vibrant-and-varied while the homepage cards stay duotone-blue — the *treatment* (halftone + cream + corner) is the through-line, not literal blue. `corepack pnpm check` passes.
  - **A.7.2 follow-up (2026-06-13) — float photo-mat.** Floated images breaking out over the hero photo now carry an opaque page-cream mat for visual separation from the halftone — size is the tunable `--image-mat` token (set to 20px after the user's tuning). Implemented in shared `_image-block.scss` (affects all article floats, not just case studies): `image-float-frame` adds `box-sizing: border-box; padding: var(--image-mat); background: var(--color-surface-warmer)`, and the same `--image-mat` is subtracted back out of every float margin (vertical in `image-float-frame`, wrap-side and breakout-side in `image-float-left/right`) so the image's position and its gap to wrapping text are preserved — the mat replaces transparent margin with opaque cream. Invisible in-column (cream on cream); visible only over the photo. `--image-mat` drops to `0px` in the phone/tablet-down float resets, so stacked floats (auto-margin column) get no mat and no shrink — matching the user's "skip the auto-margin images" rule. Non-float images (default/centered/wide, all auto-margin) and full-bleed are intentionally unmatted. Browser-verified at 1366 (mat shows over the photo, image left edge held at x195) and 375 (mat 0). `corepack pnpm check` passes.
  - **A.7.1 follow-up (2026-06-13) — group content rendered empty in the float wrap.** After A.7, the floated image had the `core/group` beside it but the group rendered with **zero children** (wrap div height 0), so no text wrapped. Cause: `FloatBreakoutGroup` passed only the breakout *subset* (`[group]`) to its inner `BlockChildren`, but a container block needs the **full flat block list** to resolve its own descendants by `parentClientId`. Fix: `BlockChildren` gained an optional `renderBlocks` prop (explicit sibling subset to render) while `blocks` stays the full flat list handed down as `all-blocks`; `FloatBreakoutGroup` now passes `:blocks="allBlocks" :render-blocks="blocks"`. Verified at 1366: the group's paragraph renders and its text lines dodge the float (first line at x603 vs. float right 579, 24px gap) — true wrap. `corepack pnpm check` passes.
    - **Open for the user's eye:** the column is *centered* (photo on both sides — a narrow strip on the left, larger area right). The original sketch was a *left* column with photo only on the right. "Center column" matched the user's latest words, but if they want the left-column read, the move is to left-align the article column for layered case studies (touches the content-flow shell) or reduce plate width so the photo's left edge meets the column.
  - **A.4 amendment (same day) — the paper top actually works now (browser-verified).** A.3 shipped the shaped background but the body never rose over the photo, so there was nothing for the transparent zone to reveal — "one opaque slab." Root causes found by live inspection: (1) base `.hero { overflow: hidden }` + the header's negative margin collapsed the hero shorter than the plate and clipped the photo; (2) `.content` had no actual pull, so it sat below the photo. Fixes: layered hero is `overflow: visible`; the title column is now `position: absolute` (bottom-anchored) so it no longer collapses the hero height — the **plate alone defines hero height**; `.content.has-paper-top` gets `margin-top: calc(-1 * var(--paper-step))` so the body genuinely rises over the photo's lower region, and the rise distance == the cream tab height by construction (the tab covers exactly the over-photo portion). Title bottom offset (`--hero-shelf-height`, default 240) is tuned to meet the rising body top → continuous column; "Title offset / shelf height" control relabeled. **Phone** falls back to a clean stack (photo band → title → body, no overlap, plate radius dropped) — verified at 375 with no horizontal scroll. **Cascade gotcha recorded:** the layered phone `.content` override had to be placed *after* the desktop `.content.has-paper-top` rule in source; at equal specificity an earlier media-query rule loses even when it matches. Browser-verified at 1400 + 375. `corepack pnpm check` passes.

- **Detail hero Phase A.1 — the carved plate (2026-06-12). REVIEWED: vetoed as a direction; variants retained in the panel for comparison.** `pages/case-studies/[slug].vue` hero rebuilt. Three layout variants behind the spike panel (now "Hero composition / duotone controls"): **carved** (default — full-width plate; the title shelf is page ground carved into a bottom corner with one drafted **elliptical** arc, sweep × shelf-height declared explicitly so the browser can't clamp a long sweep against a short shelf), **mount** (specimen mount: plate docked to one side, title in the cream column), **mount-carved** (mount plus a carved plate corner). The shelf has no border and no plate styling — it *is* the page (the plate-dissolves-to-ground thesis), and it doubles as the slip-target rectangle for Phase B. Title sits at the shelf's bottom where the arc has fully receded, so type never touches the curve; title-only, no excerpt; ink on cream — legibility unconditional. Resting color mode is **full CMYK halftone** (duotone "off") per the color journey: duotone at browsing distance, color on arrival. New knobs: layout, carve side, carve sweep (120–900px), shelf height/width, mount width; all duotone comparison knobs retained; cream-title toggle removed (title is always on ground now). **Browser-verified** (1440 + 375): carve renders with `420px 176px` elliptical radius, transition hooks (`data-featured-slip-target`, `data-featured-title-target`) present, no hero overflow at any width (one `border-box` fix needed on the shelf — the global-reset footgun again). `corepack pnpm check` passes.
  - **Check:** `http://127.0.0.1:3001/case-studies/design-system-operations-at-uscis` (and the others). Does the carve read as a drafted compass arc — committed, not "rounded corner"? Does the title-in-cove composition land?
  - **Check:** full color as the resting state — right call, or does any study's image want duotone? (Compare via the panel's mode select.)
  - **Check:** the mount and mount-carved variants via the layout select; carve side left vs right.
  - **Check:** card → detail transition + return. Phase A did not touch the transition; the clone still flies to the old geometry — expect the landing to feel unresolved until Phase B does the radius/color morph and plate-to-ground dissolve.
  - **Check:** phone — shelf at 90% width, arc clamped to 64vw; verified clean at 375px.
  - Known, out of scope: nested-list blocks in the article *body* overflow ~21px on phones (pre-existing content-flow issue, spun off as its own task).

- **Score v7 — text-dominant rows, photos as the interruption (2026-06-10, user's sketch).** The inversion: text plates are the steady vertical rhythm; each case study's own hero image interrupts horizontally. `CaseStudyCard.vue` grew layout variants delivered as props from the list's beat score: **banner** (full photo band over the text row — the editorial split), **photo-left** / **photo-right** (short text-height row with a small photo plate docked at one side, `clamp(180px, 26%, 340px)` wide, cropping to the row per the cut-to-fill contract), plus **plateAlign** (text block docked left or right within its plate). Bookends are banners (opener text-left, closer text-right per the sketch); middle phrase cycles photo-left/photo-right with an occasional banner, holding 3–8. The list is a plain full-width stack again — 12-column grid, flanking figures, and the Haeckel material all retired from this section (components stay on disk unused). The six cycling seed presets are **restored verbatim** after the v6.3 monochrome veto. Launch-case 4: banner-L → photo-left → photo-right → banner-R, exactly the user's sketch. Phone: inline rows stack as mini-banners (photo band above text). `corepack pnpm check` passes.
  - **Check:** homepage Selected Work. Does the section now read as text-rhythm rows with photographic interruptions — henry-archive anatomy with your own materials — clearly distinct from the bento below?
  - **Check:** the inline rows — is `clamp(180px, 26%, 340px)` the right photo presence beside a text row? Is the text-height row (`clamp(140px, 18vh, 220px)`) too thin/thick?
  - **Check:** plateAlign-right rows — does the right-docked text read as rhythm, not accident?
  - **Check:** restored treatment variety across the six presets — now playing across banner *and* small inline plates; do the bleed/tritone presets still earn their slots at inline scale?
  - **Check:** transition from a banner card and an inline card + returns (the slip-source plate geometry now varies a lot more per layout).
  - **Check:** phone — mini-banner stacking, wrapped titles.
  - Knobs: beat phrase in `CaseStudyList.vue`; layout geometry (column clamp, row heights, borders) in `CaseStudyCard.vue`; treatments in `HomeSelectedWorkSection.vue` `SEED_PRESETS`.
  - **v7.6 amendment (same day) — overflow actually fixed, browser-verified.** The single-line title is a hard requirement (v7.5's wrap-as-fallback vetoed). Inline rows moved from grid to **flex**: the photo plate is the flexible element (`flex: 0 1 var(--inline-photo-width)`, `min-width: 0`) and the text cell can never go narrower than the title (`flex: 1 1 auto; min-width: fit-content`, with `contain: inline-size` on the excerpt so only the title drives intrinsic width) — long titles take width from the photo instead of wrapping or overflowing. Browser inspection then found the **real root causes** of the scrollbar: (1) `.link-box` had `width: 100% + 48px inline padding` with no `border-box` anywhere — **the project has no global box-sizing reset** — overflowing every banner card by 48px; fixed by dropping the redundant `width: 100%`. (2) On phones, four homepage sections (`HomeSelectedWorkSection`, `HomeEmployerTestimonials`, `HomeLatestWritingSection`, `HomeSideProjectsLink`) full-bled with `margin-inline: -space-4` against `.home-page`'s `space-3` padding — 4px past the viewport per side; all four aligned to `space-3`. **Verified in-browser at 1900 / 1440 / 1265 / 820 / 375px: zero overflowing elements, no horizontal scrollbar, every title exactly one line on desktop.** A `.claude/launch.json` was added for the preview tooling. Note for a future pass: the missing global `border-box` reset is a standing footgun beyond this spike.
  - **v7.5 amendment (same day) — horizontal-overflow fix.** Rows wider than 100vw / clipped right-docked titles, three stacked causes: (1) `white-space: nowrap` titles can't fit inline rows' fractional text cells (40% on the wide tier) and poured into scrollable overflow; (2) `.plate-content` is a flex item in inline rows and flex's `min-width: auto` default refused to shrink it below the nowrap title; (3) long excerpt URLs (Travis) are unbreakable strings in narrow cells. Fixes: `min-width: 0` on the text block; **inline rows get a smaller fluid title (`clamp(1.25rem, 1.8vw, 1.9rem)`) allowed to wrap as a last resort** — banner rows keep the strict single-line contract, which is mathematically safe there because the viewport-clamped font stays shorter than a full-width plate; `overflow-wrap: anywhere` on excerpts. If strict single-line on inline rows ever becomes a hard requirement, the follow-up is container-query-based per-card title scaling.
  - **v7.4 amendments (same day):** wide-tier px cap removed (`60%` bare — photo plate wider than text plate is the tier's invariant, and the 900px max broke it on wide viewports). **Photo-treatment ACF radio added** — the second half of the CMS-authorability item: `selected_work_photo_treatment` (auto / blue+cream bleed / ink+blue duotone / tritone / blue+cream duotone / ink+blue crisp / tritone bleed) in the same "Selected Work Display" group, exposed as `selectedWorkPhotoTreatment` on the `CaseStudy` GraphQL type. `SEED_PRESETS` in `HomeSelectedWorkSection.vue` became the named `TREATMENT_PRESETS` map (keys match the ACF values) + `TREATMENT_CYCLE` for the auto fallback; a per-study choice pins the preset, auto cycles as before. **Editor QA:** set a treatment on one case study and confirm the homepage card changes while others keep cycling.
  - **v7.3 amendments (same day):** layout vocabulary settled per user naming — **auto / banner / narrow photo left / narrow photo right / wide photo left / wide photo right**. The wide tier (formerly "square") now puts the photo at **60% of the row width** — a bare `60%`, no px cap, after a `900px` max let the text plate outgrow the photo on wide viewports. The wide tier's defining invariant: photo plate wider than text plate, always. Taller row unchanged. **Narrow widths jitter by row index** (~30–41% of the row across a 5-step cycle) so consecutive narrow rows never line up, even if every study were set to the same CMS layout. ACF UI decluttered: ASCII legend removed (too wide for the sidebar), plain short labels, one-sentence instructions. Beats now carry a `photoTier` (narrow/wide) and geometry resolves at render; stored values from the earlier vocabulary (`photo_left`, `square`) are unknown to the new map and safely fall back to auto — re-pick in wp-admin if any were set during testing.
  - **v7.2 amendments (same day):** square beat flipped to **photo-right / text-plate-left** (review caught it running two photo-left rows back to back; note the square's final form ended up matching the user's *original* sketch). **CMS composition controls added** — the queued CMS-authorability item, composition half: ACF radio field group `group_my_website_case_study_display` ("Selected Work Display", side position) on the `case_study` CPT with `selected_work_layout` (auto / banner / photo_left / photo_right / square — instructions carry the project's `#`/`–` ASCII legend as a `<pre>`) and `selected_work_text_align` (auto / left / right). Exposed on the `CaseStudy` GraphQL type as `selectedWorkLayout` / `selectedWorkTextAlign` (default `'auto'`), queried in `caseStudiesQuery`, and applied in `CaseStudyList.vue`: `'auto'` defers to the score; a CMS layout choice replaces the beat's layout/geometry (square maps to the shared `SQUARE_*` constants); a CMS alignment choice overrides `plateAlign`. PHP `php -l` clean; `corepack pnpm check` passes. **Editor-side QA needed:** open a case study in wp-admin, confirm the radios + ASCII legend render correctly in the sidebar, set a non-auto value, and confirm the homepage row changes.
  - **v7.1 amendments (same day, from first positive review — "this is not bad!"):** inline rows ~40% taller (base `clamp(196px, 25vh, 308px)`); inline photo plates ~40% wider with **per-beat width variance** (`photoWidth` on each beat, 32–44% range, delivered as `--inline-photo-width`); new **"square" beat** per the user's (corrected) sketch (`###01###--01--` ×2) — a taller **photo-left** row (`rowHeight: clamp(340px, 40vh, 520px)`, photo 44%) with the squarer text plate on the right, still reading as a full-width row, slotted third in the middle phrase. Mid-phrase banner moved to the phrase tail so short counts (4–6) don't end on adjacent banners.

- **Score v6 — shared rows: figures flank inset cards (2026-06-10). SUPERSEDED by v7 before full QA;** kept below for the amendment history. Fixes v5's three review failures. The list is now a **12-column grid**: card slots are column spans (full = 1/-1; insets leave columns open on one side), rows stack tight, and a figure plate rendered *before* an inset card in source order auto-flows into the **same grid row**, filling the columns the inset leaves open — the row is genuinely broken into two materials, not interrupted by another row. Mockup: figure A in columns 1–3 beside the right-docked card 02; figure B in columns 9–12 beside the left-docked card 03. Figures now run the **same halftone + crisp blue-cream duotone treatment** as the cards (one register coarser, 14px screen vs. 11px), so they belong to the printed world; what distinguishes them from clickable cards is **structure, not material** — no text plate, no ordinal, no hover response, `pointer-events: none`. Figures stretch to their row's full height with mounting air (`--space-4` cell padding). Phone: figures come out entirely (`display: none`) — there is no "beside" on a stacked column, and figures-as-rows was the v5 failure. `corepack pnpm check` passes.
  - **Check:** homepage Selected Work. Do the shared rows read as henry-archive-style broken rows — card and mounted plate sharing a band — rather than more stacked content?
  - **Check:** the affordance question that killed v5 — with halftone applied but no text plate/hover, is it now obvious the figures aren't clickable entries?
  - **Check:** the 14px screen on the figures — coarse enough to read as plate-material, fine enough that the Haeckel line work survives? (Knob in `CaseStudyEditorialFigure.vue`.)
  - **Check:** judge placement/scale, not the mock material (untreated AI stand-ins behind the halftone; real material would be curated public-domain Haeckel plates).
  - **Check:** transition from an inset card and a full card + returns; phone (figures absent, height rhythm only).
  - **Fallback, pre-agreed:** if this still doesn't land, the simple left/right ping-pong is a one-edit change — set `MIDDLE_PHRASE` to alternating bare-inset beats with no `figure` entries. Everything else (grid, spans, heights, transitions) already supports it.
  - **v6.3 amendment (same day) — card treatments unified.** The six cycling spike seed presets in `HomeSelectedWorkSection.vue` were the remaining patchwork: the bleed/tritone/tint modes bypass the duotone gamut and leak raw CMYK color (the red Job Corps card, the green/pink Test card, the un-halftoned gradient on USCIS in review screenshots). All cards now run the **direct (linear) duotone** — the same family as the figures — with tone as the only variance: ink-blue on full-width bookend cards, blue-cream on flanked middles. This executes the homepage side of the queued "narrow the halftone variant set" item. The crisp/bleed/tritone machinery and SVG defs remain in the codebase for the detail-page decision.
  - **v6.2 amendment (same day):** portrait figure images were driving their rows' heights (intrinsic aspect sizing inflated rows past the card, leaving cream voids under cards and apparent overlaps). Contract settled with the user: **figures are compositional material, not content — the card alone defines the row height and the plate crops (`object-fit: cover`) to whatever space it's given.** Implemented by absolute-filling the plate inside its slot (`position: absolute; inset: var(--space-4)`); the slot is just a positioning context and contributes nothing to row sizing.
  - **v6.1 amendments after first look (same day), three bugs from screenshots:** (1) *Sparse-inkblot figures* — the threshold halftone chain reduced line-art plates to scattered blobs (contrast(1000) is tuned for photographic tonal mass, not engravings); figures now use the **linear duotone only** (`#halftone-tone-blue-cream`, the matrix's "direct" mode) so line work survives and the palette is still enforced. Crisp/dot variants remain a knob if the blend needs more tooth. (2) *Collapsed figure sliver* — percentage heights collapsed inside the stretched list item; figure slots are now `display: grid` so the plate stretches to row height by grid alignment. (3) *Dead air around unflanked insets* — the score now enforces **rows are either full-width or card+figure pairs**: every inset beat carries its flanking figure with an exactly-complementary column span (`RowBeat` structure in `CaseStudyList.vue`); the centered preset (two voids, can't flank both) was dropped from the phrase.


## Done

- **2026-06-02 → 06-03 — halftone implementation + exploration.** Pure-CSS leanrada halftone built end-to-end across detail hero, card, and transition layer; CMYK-faithful, soft separate-K, SVG duotone/tritone post-passes, chromatic aberration, and duotone-bleed variants explored; converged settings recorded under Notes below. Conclusion: technique works, doesn't fit Blue Atlas as the dominant register, doesn't solve text-on-image AA. (Commits `4039799`, `c807e73`.)
- **2026-06-04 — nine-variant settings matrix** captured against two case-study images (`temp-ref-assets/hero-comparison.pdf`) and reviewed; surfaced the figure-ground-inversion family and the subject-robustness findings. Full writeup in the conceptual doc.
- **2026-06-10 — spike controls expanded** for side-by-side text-legibility and photo-treatment comparison (commit `e3774eb`); these controls were the affordance that made the direction decision possible.
- **2026-06-10 — direction decision: (A) editorial-split wins** over (B) figure-ground inversion. Rationale: unconditional solid-ground legibility, specimen-plate register fits Blue Atlas, halftone preserved where the durable lessons said it belongs (image-area styling).
- **2026-06-10 — card converted to editorial-split** (working tree, uncommitted): `CaseStudyCard.vue` grid split with cream text plate, signal-blue ordinal badge, hover color-reveal; floating/specimen-plate/bleed-band layout variants and cream-title toggle stripped; `HomeSelectedWorkSection.vue` live controls replaced by six cycling `SEED_PRESETS` plus SVG duotone filter defs.
- **2026-06-10 — composition brainstorm** (the henry reference site): diagnosed the slab read as a silhouette problem, not a content problem; ranked variance axes (silhouette > plate-anchor > margin ordinal, halftone coarseness as supporting voice); recorded the BTAK discard pile and the held-not-discarded list in the conceptual doc; adopted the phased plan (silhouette score first).
- **2026-06-10 — silhouette score v1 (width insets) built, then vetoed at review.** First cut varied card *width* (insets 56–78% at varying anchors). The user flagged that width variance reads as bento — colliding with the upcoming bento-writing grammar for Latest Writing — and that the henry archive's strength is that it still reads as horizontal *bands*. Pivoted to the band-preserving score (v2): full-width bands, height registers, caption-plate drift. Durable lesson recorded in the conceptual doc: each homepage section needs its own compositional grammar. The height-register mechanism (`--card-min-height`) survived from v1 unchanged.
- **2026-06-10 — silhouette score v6 (shared rows, flanking figures) built, then vetoed at review.** Despite the v6.1–v6.3 fixes, review verdicts: right-side figure slots showed gaps (Job Corps, Lodestone rows — mooted by the v7 pivot before diagnosis); the v6.3 treatment unification was vetoed ("I liked that variety… a little overly monochrome") and the six-preset cycle restored; and decisively — "a series of randomly inserted illustrations… not fully distinct from the bento box below." The structural insight that followed became v7: the reference archive is *text* rows interrupted by illustrations; ours were image rows interrupted by more images. Inverted: text-dominant rows, the case studies' own hero images as the interrupting material, no borrowed figures. Haeckel plates remain a recorded tool for other surfaces.
- **2026-06-10 — silhouette score v5 (varied widths + figure rows) built, then vetoed at review.** Three critiques: (1) figures were hard to distinguish from clickable cards; (2) figures rendered as their own rows merely *interrupted* the stack instead of *breaking* rows the way the reference archive's flanking blocks do; (3) figures ran untreated and needed the halftone material language. All three addressed in v6 (shared grid rows, halftone treatment, structural-not-material distinction). User also signaled fatigue: "beginning to think we'll just need rows with a left right ping pong" — recorded as the pre-agreed fallback, one edit away.
- **2026-06-10 — silhouette score v4 (center label band) built, then vetoed at review.** Photo plate filled the card; full-width cream label band sliced the vertical center, bordered top and bottom; text drift kept. User verdict: "I don't think the center vertical text plate is working either." The dot-field interstitial strips were judged in the same pass: "not adding much" — unwired from the list (`CaseStudyInterstitialStrip.vue` retained on disk, unused; delete at spike close if still orphaned). Durable yield from v4: the title single-line / excerpt-90ch typography contract, which carries forward.
- **2026-06-10 — public-domain Haeckel plates chosen over generated imagery** for the editorial-figure material: circumvents the generic-AI-art concern entirely, period-authentic to the naturalist register, license-clean. The Midjourney plates remain useful as mockup stand-ins only.
- **2026-06-10 — silhouette score v3 (full-width plates, text drift) + interstitial strips reviewed.** v3 verdict: uncertain — flush stacking fixed the gap leakage, but the drifting text alone didn't resolve the slab feeling. Strip verdict: reads as intentional, but as an interstitial line it's "just another slab of visual content" — it doesn't break the block rhythm. Both superseded-in-place by v4 (center label band, user's proposal); the strip survives into v4 on probation. A v3-era regression was also caught and fixed in v4: the `.plate-content` width cap had broken the title's single-line contract (titles were wrapping) and squeezed the excerpt below its 90ch width.
- **2026-06-10 — editorial illustration accepted as a tool in the bag.** The user generated Midjourney test plates (Haeckel-radiolarian and engraved-botanical registers, very much the naturalist field-book read). Verdict: not *the* solution, but editorial illustration is now an available material — with edits required (strip the AI-gibberish text, color-correct to signal blue/ink/cream) and the standing generic-AI-art guard. Recorded in the conceptual doc.
- **2026-06-10 — silhouette score v2 (docked caption plate) built, then vetoed at review.** The narrowed bordered plate (border-window + hard shadow, cream beside it) "looks kind of weird" — breaking the plate out of the band undercut the slab unity the editorial split had established. Pivoted to v3 on the user's suggestion: both plates stay full-width and only the *text block inside the plate* drifts between stops. The stop mechanism (`--plate-margin-left/right`) survived intact — it just moved from the plate to the inner `.plate-content` block; the list's score was untouched. The image band keeping the ink ground/clip-path (a v2 byproduct) was left in place — it's equivalent visually and keeps the clipping where it's actually needed (the halftone panes).

## Notes / scratch

### Converging settings (in-progress — 2026-06-03)

After extensive iteration on the leanrada halftone implementation, the working settings the user has tuned to:

```
Size:          8px
Bleed:         0.45
Contrast:      1000
Sepia:         0.35
Saturation:    1.50
K image br:    0.80
K mode:        Soft (highlight detail) — see below
```

**Soft K vs Crisp K**: the user landed on **soft K** as the resting choice. Soft K diverges from leanrada's faithful implementation by stripping the threshold filter chain from the K pane — instead of `brightness() blur() contrast(1000) blur()`, the soft K pane uses just `blur() blur()`. This makes the K layer carry continuous-tone luminance that multiplies with main as a soft shadow plate, preserving highlight detail. K dots become soft gradient blobs in mid-tones/shadows instead of crisp threshold dots; the main pane still carries the crisp CMY halftone aesthetic.

Trade-off the user named explicitly: soft K gains highlight detail but loses some vibrancy compared to "no K" mode. Both look good for different purposes.

### Reserved alternate: "No K" vibrant variant for hover/active states

The user noted that **No K layer + same other settings** has a punchier, more vibrant read that could work well as a *hover state* (or other transient state) on top of the soft-K default. The vibrancy/highlight-detail trade-off becomes a useful dynamic gesture: soft-K detail at rest → no-K pop-art vibrancy on hover. Worth holding for the Phase 2 transition animation work — could be wired alongside the dot-density animation.

Settings for the "No K" hover-state variant: same as above except K mode = Off.

### Structural facts about leanrada CSS halftone (durable lessons)

These are worth folding into `docs/visual-design.md` when the spike closes:

- The technique is fundamentally posterizing — contrast(1000) thresholds each color channel to 0/1, producing at most 8 RGB primaries per pixel. Per-pixel highlight detail is structurally not possible; detail is carried by cell-level dot-size modulation and (in soft-K) by continuous K shading on top.
- Sepia at the outer box is leanrada's softening pass that desaturates the harsh CMYK primary output into "muted printed photo" register. Without sepia (or some equivalent saturation drop), the output reads pop-art.
- The two rotated ink planes (`::before` at 30°, `::after` at -21° + translate) need much more pseudo-extension than leanrada's `inset: -30%` for wide aspect ratios — we use `inset: -100%`.
- The K layer's structural role is shadow density, not highlight detail. The "highlight detail" the user wants is best served by either (a) the soft-K continuous-tone shading multiplied with main, or (b) accepting the threshold trade-off and using vibrancy/contrast as the visual signature instead.


- Trail/echo animation from `gendes-seamless.codex` is a future consideration only — phase 2 starts with halftone-density animation on the existing FLIP morph, not a new motion system.
- BTAK reminder: no "CASE STUDY · 2024" or similar labels on the panel/title. Materials, not labels.
- The user has noted current case study images (Travis County, USCIS, Job Corps) are uneven in quality. A halftone filter forgives mediocre source material more than a clean composition does — this is a practical benefit of the filter direction.
- The body-text overlap reference is the author's older desert-jackalope portfolio in `temp-ref-assets/`. The overlap candidate has been **moved out of this spike**; it now lives in the bento-writing spike as a candidate for writing-detail frame-breaking. Phase 3 here is rounded-corner-or-equivalent, not overlap.
- The writing detail page (`apps/frontend/pages/writing/[slug].vue`) is **out of scope** for this spike. The bento-writing spike owns writing-side hero treatment so the homepage writing section and the writing detail page can be redesigned coherently together.
