# Accessibility And SEO Pass 2 — Tasks
## Background
Final correctness audit before shifting emphasis to authored content. Conceptual scope: `docs/active-spikes/wcag-seo2.md`. Performance measurements stay in `docs/scratch/performance-optimization.md`.
## General Principles
- Audit an identified production artifact; distinguish deployed results from local fixes.
- Preserve visual character and the custom overlap transition. Fix evidenced barriers rather than optimizing scores alone.
- Keep QA fixtures out of public publishing. Never infer accessibility compliance from automated scores.
## Current State Overview
Audit started 2026-09-09 after the approved animation/housekeeping commits and a verified clean working tree. Animation is archived by human acceptance. The subsequently reported case-study strobe and distorted photoplate geometry were repaired under S6/S9; on September 9 the user confirmed no strobing or weird stretching. That regression is no longer an open visual-QA blocker. The fixes have not been deployed by the agent. First observations below are a bounded production baseline, not a compliance result.
## Initial Production Observations — 2026-09-09
- Production origin: `https://www.aslanfrench.work`. HTTP homepage Last-Modified: 2026-09-08 02:31:01 GMT; sampled at approximately 15:18 UTC on September 9. Exact release manifest identity is not yet established, so S1 remains open. Local HEAD includes later changes and must not be treated as the deployed artifact.
- Homepage browser DOM: `lang=en`, one h1, one self-canonical, expected description and production social-image URL, one main landmark and labeled Footer navigation.
- Public HTTP checks: homepage 200; apex 301 to www; `/audit-nonexistent-page-20260909` returns 404; default social PNG returns 200 with image/png. Robots allows production crawling and references the correct sitemap. The inspected sitemap contains production-origin content routes and no visible dev/QA routes.
- Writing sample: `/writing/design-principles-for-enzo-a4f9af10cd03` has one h1, logical sampled h2 structure, no images missing an alt attribute, and the expected external Medium canonical. This does not establish alt-text quality or complete accessible naming; a simple empty-button heuristic found no candidates.
- Finding for S8: that externally canonical writing URL is also included in the local sitemap. Review canonical filtering in sitemap generation; do not remove the CMS cross-post canonical or change editorial ownership to make the check pass.
- Footer repeated ticker text initially appeared duplicated in raw DOM text; local source marks repeated copies aria-hidden. Do not report a screen-reader duplicate based on textContent alone; verify the deployed accessibility tree during S4.
- S2–S6 remain incomplete: a few DOM/keyboard observations and transition checks do not replace automated scanning, full keyboard tasks, assistive technology, responsive reflow/contrast, or continuous-motion review. No compliance claim is made from this baseline.
## Coverage And Audit Method
Record exact URLs and artifact identity under S1 before execution. Minimum route/state coverage:

| Surface | Required states and tasks |
| --- | --- |
| Home | Cold load and client return; BLUF disclosure; construction banner; case cards; writing bento; testimonials; footer; pointer and keyboard equivalents |
| Writing archive | Cold load, pagination/load more, card navigation, return from a later article, restored focus/scroll |
| Writing detail | Original and externally canonical posts; long article with TOC/footnotes; header/body/footer and margin organisms; direct entry and transition entry |
| Case-study detail | USCIS and Travis County or equivalent different image geometry; Home↔detail and both loop directions; lightbox/gallery open, zoomed, advanced, closed |
| About / Now / Side Projects | Each route directly and via internal navigation; CMS heading/body; Now single-source content; footer |
| QA-only block fixture | Image alt variants; galleries and video lightboxes; media/text and floats; wide/full blocks; tables; code; accordion/details; audio/video; embeds; footnotes |
| Failure/fallback states | Real static 404; empty collection and missing optional fields on QA; loading/error announcements where present; broken-media fallback without corrupting public CMS |

Primary browser coverage: current desktop Chromium full pass, Firefox and Safari targeted reflow/focus/lightbox/transition checks, Android Chrome touch and TalkBack task checks where available. Record versions and unavailable combinations. Test 320 CSS-pixel reflow, phone/tablet/desktop including intermediate breakpoints, 200% text enlargement, 400% browser zoom where applicable, text-spacing overrides, and portrait/landscape. Test normal and reduced motion; inspect forced-colors/focus behavior as supplementary robustness. Do not claim the full route×browser Cartesian product was tested when only representative tasks were.

Evidence record for every finding: ID, route, artifact, browser/device, UI state, steps, expected/observed result, criterion or SEO rule, severity, ownership, status, repair, retest. Use critical/high for unsafe motion or blocked tasks, medium for meaningful barriers, low for polish; automated severity is a starting point, not the disposition. Track criterion coverage separately so non-applicable form/authentication criteria do not become invented work. Keep bulky reports/screenshots in ignored local audit storage; commit concise reproducible findings here or in a linked audit report.
## Preliminary Leads To Revalidate
These are observations from the interrupted first pass, not a completed audit or newly verified failures:
- A production case-detail footer exposed local-origin internal links while Home appeared normalized. Recheck per-route generated HTML/payloads and deployed pages; newer static inspection passed, so do not assume the source still has the historical defect.
- An opened PhotoSwipe dialog had no observed accessible name. Verify computed name, modality, focus trap, Escape and restoration; absence of one ARIA attribute alone is not the verdict.
- Repeated generic fullsize-image button labels and empty alt values need contextual review. Distinguish decorative images from meaningful slides and CMS omissions from renderer loss.
- Initial keyboard entry reached BLUF; check bypass navigation and route-change focus intentionally. A missing skip link alone does not prove failure if another sufficient bypass exists.
- Footer ticker and other persistent motion need an applicability/pause review after Motion QA removal. Do not treat inaccessible debug controls as a visitor-facing solution.
## To Do
- F3. Resolve the footer ticker's missing pause mechanism (medium, WCAG 2.2.2 review). Live footer has a continuously running 32-second text marquee with no pause control; the only Home buttons observed are BLUF and banner dismiss. Reduced-motion CSS exists but does not establish a usable pause mechanism in the default presentation. Propose minimal visitor-facing motion control with the user before changing chrome. Repeated ticker copies are correctly `aria-hidden`; do not report duplicated raw DOM text as an assistive-technology failure. [Criterion reference](https://www.w3.org/WAI/WCAG22/Understanding/pause-stop-hide.html).
- F6. Complete route-focus and image-description review (investigation, not yet a confirmed failure). Keyboard archive→Enzo navigation ends with focus on body, while document title updates correctly. TOC Names and Values updates the hash and places the heading at 69.5px from the viewport top, but retains focus on the TOC link. Verify subsequent Tab and actual AT behavior before selecting a focus repair. USCIS exposes 18 identically named fullsize buttons; all 21 images have empty alt, including decorative plates. Seven Side Projects images also have empty alt. Determine which content needs editorial alternatives and whether the renderer preserves CMS values; do not invent descriptions or label every decorative image a violation.
- S1. Record release/build provenance and a representative route/device matrix, including cold entry and client navigation.
  - S1a. Identify current Bunny release from retained manifest when possible; otherwise record timestamp/headers and explicitly unknown identity. Name the current local generated artifact separately, including CMS source, deploy environment, source revision and generation time.
  - S1b. Resolve concrete routes for the matrix above; inventory all interactive components and block types. Record supported, unavailable and not-applicable states. Use a production-mode static artifact for final metadata assertions; never expect preview robots to be indexable.
- S2. Run automated accessibility checks on representative settled routes and open UI states; triage actual violations with reproducible evidence.
  - S2a. Establish a reproducible axe-based scan or equivalent allowed tooling; record tool/version and save reports. Scan initial pages plus open navigation, TOC, accordion and PhotoSwipe states, not just Home.
  - S2b. Deduplicate shared-component findings while preserving affected routes; manually confirm positives and document tool limits. Add focused regression assertions for stable semantics rather than a second general testing framework without need.
- S3. Manually test keyboard order, visible focus, disclosures, TOC, pagination, lightbox focus trapping/restoration, media controls, and browser Back.
  - S3a. Complete tasks using Tab/Shift+Tab, Enter/Space, Escape and applicable arrow keys. Check bypass, no traps, no hidden focus stops, focus not obscured, anchor target position, and no essential hover-only actions.
  - S3b. Verify route-change focus, page title announcement, load-more focus, lightbox opener restoration, and browser Back with transition clones excluded from interaction/accessibility. Preserve destination-first visual overlap while preventing duplicate focusable copies.
- S4. Test screen-reader names/roles, landmark and heading structure, link purpose, image alt handling, errors, and loading states.
  - S4a. Use an actual available screen reader for landmark/heading navigation and representative disclosure, lightbox and article tasks. Record exact AT/browser; leave TalkBack or unavailable sessions explicitly pending rather than infer results from DOM snapshots.
  - S4b. Audit informative/decorative alt decisions, icon control names, visible-label/name agreement, split-letter headings, decorative cursor/canvas/trail exclusion, ticker duplicates, iframe titles, tables and footnote relationships. Verify media caption/transcript support and identify missing editorial assets without fabricating them.
- S5. Check contrast, text zoom/reflow at narrow widths, touch target usability, and content legibility with representative long titles and blocks.
  - S5a. Measure text and meaningful UI contrast in normal/hover/focus/disabled contexts as applicable, including duotone/photoplate and quote layering. Check text-spacing overrides, enlarged text, responsive wrapping, scrollable code/tables, dialogs and TOC without clipped controls.
  - S5b. Evaluate AA target size/spacing with applicable exceptions, non-drag alternatives to essential drag functions, touch zoom, orientation, and custom-cursor readability/native fallback. Do not demand decorative artwork meet text contrast rules.
- S6. Verify reduced-motion alternatives and review continuous animation/flash exposure, including the repaired case-study transition. Identify any required pause controls without assuming prefers-reduced-motion alone suffices.
  - S6a. Inventory RD background/margins, wave, stars, ticker, quote wash, banner GIF, proximity/parallax and route trails. Test preference on initial load and change; ensure controls/content remain usable with motion off and tilt unavailable.
  - S6b. Evaluate continuous-motion requirements per effect and record rationale. If needed, present minimal pause-control placement/copy for approval; do not remove approved flair or resurrect Motion QA by default. Do not deliberately expose the user to strobing to test thresholds; use captured evidence/analysis if a flash concern recurs.
- S7. Inspect generated HTML for titles/descriptions, canonical and cross-post overrides, social-card URLs, JSON-LD accuracy, and exactly one page h1.
  - S7a. Check every generated public route mechanically for missing/duplicate metadata, placeholders, wrong origins and contradictory canonicals. Inspect representative HTML before hydration and after client navigation for stale head tags.
  - S7b. Validate JSON-LD syntax and factual Person/WebSite/article properties where emitted; inspect preview images' URL, dimensions and content type. Preserve approved copy; return genuine copy/identity questions to the user rather than invent facts.
- S8. Verify production robots/sitemap URLs, indexability, redirects, missing-page status behavior, dev/QA exclusions, and llms.txt accuracy. Do not change discovery settings during measurement.
  - S8a. Resolve sitemap entries against canonical policy and 200 responses; handle external-canonical posts without altering CMS ownership. Check apex/www, HTTP/HTTPS, trailing-slash behavior, internal links/fragments and custom-domain redirects without loops.
  - S8b. Verify generated routes, shared payload links, 404 status, exclusion of dev/QA content and absence of local runtime references. Check llms.txt prose/links as project metadata, not an SEO compliance requirement. Search Console indexing/submission is optional account-owned follow-up, not a blocker to code audit.
- S9. Fix confirmed findings in bounded batches; retest each and record any accepted limitations.
  - S9a. Prioritize blocked tasks and safety, shared semantics/focus, reflow/contrast, then search correctness. Each fix gets an atomic task/finding, relevant checks and commit; visible art-direction or editorial decisions require user review.
  - S9b. Run `corepack pnpm check` for code changes and matching static inspection after generation. Retest the original failure and neighboring states, including Home↔writing, archive↔writing, Home↔case and case loop; do not substitute passing lint for rendered verification.
- S10. Produce a small repeatable regression checklist and editorial accessibility requirements; prepare closeout for approval.
  - S10a. Record final criterion/route coverage, remaining exceptions and author responsibilities for alt, headings, captions/transcripts, link text and SEO fields. No known high-impact defect is silently deferred; accepted exceptions cannot be called conformance.
  - S10b. After authorized publishing, tie a production smoke pass to the actual release and verify repaired behavior, metadata, robots/sitemap and HTTP status. If no release is authorized, retain this as pending rather than equating local preview with production.
  - S10c. Carry the checklist into durable docs, link performance's required regression checks, request closeout approval and archive without condensing the task history. The performance spike remains separate and should not undo these accessibility/transition guarantees.
## Ready For Human QA
None currently awaiting approval.
## Done
- F1. Repair local-origin footer links in production (high, navigation/SEO; confirmed). All 28 generated content routes contain `http://my-website.localhost` footer destinations; live Home confirms the same. Normalize known internal CMS/frontend origins before serialization, preserve external links, and make static inspection catch local frontend hrefs as well as API references. Reproduce by inspecting Footer About/Case Studies/Writing on production; expected same-site routes, actual local-development destinations. Source evidence: `normalizeLinks` only filters entries; `SiteFooter.normalizedInternalTarget` recognizes the configured production origin/subdomains, not the authored local frontend origin. No source fix made during this audit.
  - Repaired and locally retested 2026-09-11. Structured Home/Footer CMS links now use the same known-origin normalization as body links before serialization, including configured site origins and authored local frontend origins. The generated Writing footer exposes `/about#now`, `/#selected-work`, `/side-projects`, `/writing`, and `/about#now`; no non-media local runtime references remain across the 28-route production artifact. Static inspection now treats local frontend HTML hrefs as blocking runtime references and was separately confirmed to reject the retained affected release. External links remain unchanged. This is local artifact evidence; nothing was deployed.
  - Human QA confirmed 2026-09-12: the user navigated around the local static artifact, found the site generally sound, and confirmed that all footer links they tried worked. This accepts the visible local footer-link repair; it does not substitute for the remaining systematic accessibility audit or deployed-production retest.
- F2. Give the shared lightbox dialog an accessible name (medium, WCAG 4.1.2; confirmed). On static USCIS detail, keyboard-open the first `View image fullsize` button. Settled PhotoSwipe root is an unnamed `role="dialog"`; the snapshot also exposes an unnamed dialog. `useImageLightbox` supplies only dataSource/index. Expected a meaningful dialog name without changing the approved visual design. Retest single and gallery lightboxes after repair. [Criterion reference](https://www.w3.org/WAI/WCAG22/Understanding/name-role-value.html).
  - Repaired and locally retested 2026-09-11. Shared image lightboxes expose one `role="dialog"` named `Image viewer`; the independently constructed Mega Gallery lightbox exposes `Media viewer`. Both labels are installed during PhotoSwipe's first update, before delayed keyboard focus enters the root. Chromium checks on the production USCIS artifact and QA block fixture confirmed the computed dialog names, settled focus inside each dialog, Escape close, and restoration to the invoking image button. No visual chrome changed and nothing was deployed.
- F4. Filter sitemap entries according to deliberate canonical ownership (medium SEO; confirmed generated inconsistency). Eighteen externally canonical writing posts remain listed under this site's URLs in the production sitemap. Preserve their CMS cross-post canonical and public availability; adjust discovery generation rather than claiming local originals. Reproduce by comparing each generated writing canonical with sitemap loc entries. Retest production output and ensure preview remains blocked.
  - Repaired and locally retested 2026-09-11. Finalization reads each generated route's canonical and includes only equivalent self-canonical URLs in `sitemap.xml`, treating optional trailing slashes as equivalent while still requiring the same origin, query, and fragment. The production artifact contains 10 self-canonical sitemap entries and retains all 18 external writing canonicals in their HTML; those browseable cross-post routes remain in `llms.txt`. Production robots, sitemap, canonical, and origin inspection passes. QA generation remains non-indexable. Nothing was deployed.
- F5. Correct Writing archive heading hierarchy (low, project semantic contract; confirmed). Both local static and live archive go directly from H1 Writing to H3 card titles; year labels are generic elements, not intervening H2s. Decide whether year headings or H2 card titles express the intended structure, without changing their typography. A skipped level alone is not being represented as an automatic WCAG failure.
  - Repaired and locally retested 2026-09-11. Existing visible year labels are now semantic H2 headings, retaining their typography and grouping the H3 article titles beneath them. Chromium inspection of the generated Writing archive confirmed `H1 Writing → H2 2025 → H3 posts → H2 2024 → H3 posts`.
- Unplanned static-generation repair — 2026-09-11: canonical-aware finalization exposed that `NUXT_STATIC_CMS_ENV` applied only to `nuxt generate`, so QA finalization rediscovered public routes and failed while reading public-only files from a QA artifact. Each explicit public/QA and compatibility content/dev generation command now carries its selected CMS environment into finalization. A QA artifact with 122 generated routes completed successfully and retained crawler blocking; a subsequent public production-mode artifact also completed successfully.
- Audit discovery batch — 2026-09-09 local / September 10 UTC: identified the current artifact and exercised generated Home, Writing archive, Enzo writing detail, USCIS case detail, About, Now and Side Projects in desktop Chromium at 1920×898. Browser version and other browser/device combinations remain to be recorded. No application/CMS changes or deployment performed.
  - Provenance: local `.output/public/index.html` exactly matches the retained release `20260910T034218Z-f3be5a8bd474` homepage hash and a fresh public homepage download: SHA-256 `052be9a0c54e6bbae82e4fd51778c63608025550266c2054f4b06d2578a559ab`. Manifest generation time is `2026-09-10T03:42:17.974Z`, clean source, canonical origin `https://www.aslanfrench.work`; current robots allows production. This supersedes the previous turn's assumption that `.output/public` still contained the deliberately non-indexable preview. Homepage equality identifies that route; other CDN objects can still have independent cache state.
  - Generated HTML mechanical inventory: 30 HTML files including 28 content routes plus 200/404 shells. All 28 content routes have exactly one H1, one canonical and a nonempty description. No img tag was found missing an alt attribute after correctly accounting for serialized bare `alt` attributes. Empty alt quality is a separate unresolved issue. Home JSON-LD parses; other routes do not emit JSON-LD, which is not automatically a defect or a reason to expand schema. The 200/404 shell documents have no prerendered H1/canonical; rendered error-state evaluation remains pending.
  - Keyboard observations: Home BLUF opens with Enter and exposes expanded state and section links; observed focused control outline is solid 2px blue. USCIS lightbox moves focus to its root after opening animation, reaches Close with Tab, and restores the original image button on Escape. Shift+Tab is redirected to the dialog root. Forward Tab from the last Close control produced body focus in one sample (possibly browser chrome); focus containment is not declared passed or failed until a continuous keyboard sequence is tested. Avoid drawing a trap-failure conclusion from a locator operation that refocuses its target.
  - Navigation observations: keyboard Writing archive→Enzo completes, updates the page title and leaves no temporary flight planes after settling. Enzo TOC activation updates the hash and positions the target visibly. Actual screen-reader announcement/reading position has not been tested.
  - Limitations: this was generated-HTML checks, browser DOM inspection and bounded keyboard tasks, not axe/Lighthouse or a screen-reader audit. No axe/pa11y/Lighthouse dependency is declared in either package manifest; automated scan tooling remains S2 work. Cross-browser, narrow reflow, zoom, measured contrast, preference emulation, QA block fixtures, media alternatives and actual VoiceOver/TalkBack remain open. No claim of WCAG compliance. Findings F1–F6 above are the actionable output, not a replacement for the remaining S1–S10 coverage.
- Scope promoted and separated from runtime/delivery performance work.
- S9 follow-up — remove distorted photoplate scaling: user rejected the squashed/stretched image and corner geometry. The earlier fixed-source-size/nonuniform-scale optimization was not appropriate for this art direction. Restore actual width/height and border-radius interpolation on the main CSS transition and resize the decorative WAAPI echoes the same way, preserving `object-fit: cover` cropping. No raster screenshot substitution, timing changes, or overlap handoff changes. The separate temporary-plane compositor candidate remains in place; the original strobe still needs confirmation with the restored geometry.
  - Verification: `corepack pnpm check` passed (existing About/Now `vue/no-v-html` warnings only). Rebuilt the public-CMS artifact with `STATIC_DEPLOY_ENV=preview` and passed matching preview inspection. Browser samples on static Home↔USCIS confirmed changing layout width/height, `object-fit: cover`, unit X/Y transform scale throughout, and bottom-right border-radius interpolation between 0px and 900px. Nothing deployed; final visual approval and strobe confirmation remain open.
  - Human QA confirmed 2026-09-09: no strobing and no weird stretching. Original verification notes above are preserved as implementation history; their pending visual approval is now satisfied.
- S9 follow-up — case-study mid-flight strobe: the supplied recording shows the media AND title clone absent together at approximately 3.95s/4.20s on the USCIS arrival and 10.40s on the Travis County arrival, with underlying page/body content still visible. This is not halftone shimmer or the final handoff. Production DOM samples in both directions retained the clone's opacity 1 during flight; the agent has not independently reproduced the missing painted frames. A narrowly scoped candidate repair promotes each temporary teleported flight plane to a stable composited surface for its lifetime. No changes to source media, ghost trails, geometry timing, z-order, or destination-first overlap. Retest Home↔USCIS and Home↔Travis County in the browser/environment that produced the recording, including a fresh static artifact before claiming a deployed fix. Do not mark this resolved solely from passing lint/typecheck or clean opacity samples.
  - Verification: `corepack pnpm check` passed with the existing two `vue/no-v-html` warnings in About/Now. Public-CMS static generation succeeded with `STATIC_DEPLOY_ENV=preview`; the result is deliberately non-indexable, not a production release. The first inspection used the configured production environment and correctly rejected preview robots; rerunning inspection with `STATIC_DEPLOY_ENV=preview` matched the artifact. SSR forward/reverse and fresh static Home↔USCIS samples retained the new plane transform, full flight opacity, and normal eventual clone cleanup; SSR Travis County arrival was also exercised. This tests the implementation and lifecycle, not proof that the user's intermittent paint dropout is eliminated. Nothing uploaded to Bunny. A normal production generation is required before any future production deploy.
  - Human QA confirmed 2026-09-09: no strobing and no weird stretching. This confirms the combined repair in the user's tested environment; it does not claim a new Bunny deployment.
