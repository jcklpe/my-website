# Animation & Motion To-Do
## Background
Promoted 2026-07-29 from `docs/scratch/animations.md` + `docs/scratch/conways-game-of-life.md` (both retired), plus the ambient-motion / reaction-diffusion / featured-media-transition-jank material routed out of the archived brand-voice spike. Conceptual doc: `docs/active-spikes/animation.md` — read it first for philosophy, the existing-motion-system relationship (esp. the HARD RULE to pause ambient motion during featured-media transitions), and the seven work threads (A–G).

This is a broad, taste-gated spike: pick candidates off one at a time, each behind human visual QA.

## Project Organization
- Conceptual doc: `docs/active-spikes/animation.md`
- Operational doc: `docs/active-spikes/animation.todo.md`
- Retired scratch sources: `docs/scratch/animations.md`, `docs/scratch/conways-game-of-life.md` (deleted during promotion)
- Continues from: `docs/archive/brand-voice.md`
- Related durable docs: `docs/design-system.md` and `AGENTS.md` → "Route Transition and Motion Rules"; `docs/visual-design.md` for the Blue Atlas language and the settled hero.

## General Principles
- The featured-media route transition wins; new motion must not compete with it and must pause while it runs.
- Ambient motion is slow, cheap, non-informational, off under `prefers-reduced-motion`, and paused offscreen.
- CSS and the Web Animations API first; canvas at low FPS only where genuinely needed. No general animation library is currently installed. Adding one or more small libraries remains open when a concrete parallax or choreography prototype justifies the dependency and performs better than local code.
- Autonomous motion should usually stay off information-carrying surfaces, but subtle scroll-, pointer-, entry-, and hover-driven motion on text, headings, images, lists, and cards is open when it preserves legibility and stable layout.
- Everything must survive static generation + CDN hosting and must be SSR-safe (browser APIs inside `onMounted`/client-only).
- Decide scope/priority with the user before building — this doc lists the menu.

## Current State Overview
- Site motion today: the featured-media morph; slit-slip arrows on the footnote toggle, file-download button, PhotoSwipe arrows and (as of 2026-08-05) the three "→" CTAs; and two ambient pieces — Conway on the Side Projects card and the page-wide reaction-diffusion skin. The later production trial adds the same GPU reaction-diffusion ecology to writing-detail margins; the original homepage-only state is superseded.
- The reaction-diffusion skin is the largest thing in this spike by a wide margin: a WebGL2 Gray-Scott simulation with a drifting fertility mask, cursor attraction on desktop, device-tilt response on touch, a measured dead zone behind the wordmark, and baked opening states. If you are touching it, read the `Done` entry for Thread B and the durable lessons first — several of its parameters are load-bearing in non-obvious ways, and the failure modes are well documented there precisely because they were expensive to find.
- The homepage BLUF hero is a settled responsive composition with a real ACF-swappable portrait (brand-voice, closed 2026-07-29). Reaction-diffusion in the hero is now an *alternative/companion* to that portrait, not an assumed replacement.
- `useFeaturedMediaTransitionState()` exposes an `active` flag; `pages/index.vue` already watches it for the home transition choreography — the same gate is the hook for pausing ambient motion.

## Key file pointers
- `apps/frontend/components/home/HomeEmployerTestimonials.vue` — testimonials texture background (drift candidate A1).
- `apps/frontend/pages/index.vue` — BLUF hero (grain A2, reaction-diffusion B), Side Projects link section, transition-state watcher.
- `apps/frontend/components/home/HomeSideProjectsLink.vue` — Side Projects card (Conway C).
- `apps/frontend/components/home/HomeReactionDiffusionBackground.vue` — the RD skin (Thread B).
- `apps/frontend/pages/dev/rd.vue` — the RD debug harness: mask/drift/nucleation toggles, F/k presets, raw-field views, and the influence point (mouse/touch/tilt ball) with a magenta marker and its own sliders. Kept as a possible easter egg.
- `apps/frontend/pages/dev/motion.vue` — CMS-backed motion lab with one global card-treatment selector plus text, accent, entrance, inset-depth, and margin-organism specimens.
- `apps/frontend/components/dev/MotionCaseStudyLab.vue` — wraps real Selected Work cards and their production halftone media in measured, non-destructive hover-effect layers.
- `apps/frontend/components/dev/MotionReactionDiffusionStrip.vue` — small CPU Gray-Scott specimen used by the motion lab's RD eyebrow and transparent margin organisms.
- `apps/frontend/components/home/HomeSelectedWorkSection.vue` — Selected Work label rule (pulse A3).
- `apps/frontend/composables/useFeaturedMediaTransition.ts`, `useHomeTransitionChoreography.ts`, `useFeaturedMediaTransitionState.ts` — the transition system (audit G; the `active` gate).
- `packages/styles/_motion-palette.scss` — shared timing/easing tokens.
- `packages/styles/context-role/_vue-frontend.scss` — texture token definitions.
- `packages/styles/shared-components/_slit-slip.scss` — the shared slit-slip recipe (Thread D). `@include arrow-cta-slit('.selector')` is the whole API.
- `apps/frontend/assets/rd-seeds/` — baked RD opening states; globbed at build time, so adding a PNG is the entire procedure.
- `docs/scratch/reaction-diffusion-case-study.md` — plain-language write-up of the finished RD system and what each control does. The fastest way to understand that component without reading the shader.
- Slit-slip existing callsites: footnote "more/less" button, file-download block, PhotoSwipe lightbox arrows (find via grep for the clipped-slot pattern).

## To Do
The first implementation slices—Conway (C), homepage reaction-diffusion (B), and slit-slip expansion (D)—are complete. The spike is now in final selection, tuning, transition QA, and closeout rather than open-ended ideation.

**Current priority, updated 2026-09-07 — START HERE IF YOU ARE PICKING THIS UP COLD.**

B, C, and D shipped. Android reaction-diffusion tilt is approved. Pointer proximity is the selected heading model on Selected Work, Latest Writing, Testimonials, and Side Projects, but the Selected Work accent is deliberately stable rather than joining the letter field. Catalog metadata, inset-image parallax, colour reveal, baked halftone, the broad case-study current, rotating ordinal star, low-travel Latest Writing card proximity, Latest Writing crosshair rotation, footer quiet signal, and footer heading ticker are accepted production behavior rather than QA toggles. The irregular SVG flag is the approved Selected Work accent; standalone and hybrid WebGL lava branches are rejected and removed. The original article margin organisms were rejected because their independent CPU simulation could neither match the homepage GPU ecology nor meet the transition/performance budget. A replacement production trial now reveals one shared homepage GPU ecology through soft desktop margin windows. The Latest Writing edge relay and five follow-up ambient-card candidates were all rejected and removed; the accepted low-travel proximity and rotating crosshair are enough. The title-ground blink and current phone browser-Back behavior are human-confirmed resolved. Remaining work is the margin placement correction, optional ghost-trail comparison, and final regression checks of writing-row and other transition paths.

The broad brainstorming/lab phase has done its job. Preserve the user’s goal that the site feel alive—possibly like diving deeper into a living atlas—but do not reopen rejected experiments merely to add more motion. Closeout work should tune accepted behaviors in production context, verify transitions, and then fold durable motion rules into the project docs.

- A21. OPEN: Try the transition effect trials during the current animation-spike work. Compare the resulting motion against the existing source/target transition behavior and preserve reduced-motion behavior. **CLARIFIED / READY FOR HUMAN QA 2026-09-07:** this refers to the ghost-image trails from the old generative-design branches, not a new replacement for the shared-media transition. Read-only archaeology found four GSAP echoes in `gendes-seamless.cc`, CSS trail frames in `gendes-seamless.codex`, and intro trails in `gendes-seamless.copilot`. The current trial uses native Web Animations for four image-only echoes behind the flying image, under Motion QA → Transition trial. It defaults off, respects reduced motion, uses the existing flight duration, and ends before handoff so it cannot interfere with the accepted A/B overlap. User is undecided whether the additional motion suits the site.

**E is not outstanding:** the accordion icon already swaps +/− with a 220ms rotation in the shared accordion recipe. **F** (button hover character) was never selected and is dropped.

Deferred B items (B6 mobile perf, B7 site-wide promotion, B8 scroll-coupling, B10 phone liveliness) are listed under that thread and are not blocking. B7 and B10 are the two most likely to come up during the Thread A conversation, since both are about motion beyond the homepage.

### Thread A — Ambient background motion (CSS-first)
- A1. PRIOR AGENT CANDIDATE, NOT SELECTED: Testimonials texture drift — slow `@keyframes` on `background-position` of the signal-dot field, affirmative reduced-motion opt-in. OPEN: always-on vs hover/focus-within.
- A2. PRIOR AGENT CANDIDATE, NOT SELECTED: Hero film-grain shimmer — only once a grain/noise overlay exists on the hero. `steps()` transform shimmer. OPEN: hero-only vs page-level.
- A3. PRIOR AGENT CANDIDATE, NOT SELECTED: Accent-rule pulse — very slow opacity breathing on `--color-primary` rules. Easy to tune or remove.
- A4. DEFERRED: Canvas particle / ink-diffusion ambient field. RD and Conway already cover generative simulation territory; only revisit for a specific art-direction need that those systems cannot meet.
- A5. READY FOR HUMAN QA: writing archive rows and their blue year headers now use the approved bottom-up slit with a 140ms ordered stagger on the real `/writing` page. Verify normal arrival, reverse transition arrival, load-more behavior, and reduced motion.
- A20. READY FOR HUMAN QA: `/dev/margin-organisms` is a deliberately bounded mock of the viable successor to rejected A8. It runs one instance of the actual homepage WebGL ecology and reveals that continuous field through two soft margin windows around an article-like composition. The mock therefore inherits the homepage's coral, drift, turnover, crisp rendering, and page-relative pointer response instead of attempting to imitate them in separate CPU patches. Human review approved the mock as the intended visual, so writing-detail pages now mount that same single shared GPU field as a production trial. It appears only at the desktop breakpoint, remains behind the opaque article header/body surfaces, and pauses during featured-media transitions; narrow layouts do not run it. Judge the masks against short and long real posts, including whether their fixed viewport placement feels sufficiently page-aware while scrolling. **FOLLOW-UP 2026-09-07:** human review likes the ecology but found it hidden under the full-width article ground and visible beside the hero instead. Article margins now stay hidden until 150vh of actual downward page scroll, clip to the body’s vertical extent, and pause outside that region. Desktop body paper is limited to the reading column; existing block underlap mattes protect wider content. Verify at mid-article, with wide/full blocks, and near the article end. The standalone mock retains its immediate reveal.
- A10. READY FOR HUMAN QA: `/writing` coordinates its archive entrance with shared-media reverse transitions. The returning article's row is the timing anchor. The two preceding articles use compressed 300ms slit entrances that finish at 320ms and 460ms of the 600ms flight; intervening year chrome gets its own beat, the target row finishes with the flight, and following rows continue at 140ms intervals. Home/direct arrivals start the same slit sequence independently. Clip-path preserves row geometry throughout. Verify home-to-archive, article-to-archive for targets at several list positions, direct archive entry, and load-more behavior.
- A11. DEFERRED: investigate whether the Latest Writing bento cards should eventually travel into their archive positions instead of disappearing and reappearing during navigation. Do not fold this into the current archive-sequence repair; first settle the existing row/shared-media choreography.

### Thread B — Reaction-diffusion page-wide skin over the grid paper (portrait stays)
**DONE — QA-approved 2026-08-03 on desktop, 2026-08-05 including mobile tilt. See Done for the full record and the durable lessons. Still open, deliberately deferred rather than dropped:**
- B9. DONE 2026-08-05 — five baked seeds shipped in `apps/frontend/assets/rd-seeds/`. **Workflow for adding more:** open `/dev/rd` (its defaults mirror the homepage, so what is on screen is what the page will open on), let it develop to a composition worth keeping, press **bake seed**, and drop the downloaded PNG into `apps/frontend/assets/rd-seeds/`. That is the whole procedure — the component globs that folder at build time, so there is no list to keep in sync. Bake several; one is chosen at random per visit so the opening composition varies. Nothing touches WordPress. An empty folder is a valid state: it falls back to the procedural warm-up. **This originally used a manifest file in `public/`, and the first five baked seeds silently never loaded because the manifest was left empty — the fallback looks similar enough to pass for success. The glob removes that failure mode, and a dev-only console line reports when a baked seed is actually used.** Original note: **baked starting state.** The opening frame should be a saved, already-developed state loaded instantly, not the current warm-up-behind-a-fade. Run the sim, pause at a good moment, save that state, ship it as an asset, load it as the initial texture. Workflow that fits what already exists: add a bake button to `/dev/rd` that reads the state texture back and downloads it as a PNG (u in R, v in G), commit that, and have the component load it in place of `seed()` + warm-up. Falls back to the current warm-up if the asset fails to load. Requested 2026-08-05: the current version "costs a beat of waiting rather than being instant".
- B6. OPEN: mobile perf pass — the sim is full-viewport; confirm cost on a real phone and decide whether to lower `SIM_SCALE` / `ITERS_PER_FRAME` there. (Anecdotally fine on device through the tilt QA, but never measured.)
- B7. OPEN: promote from homepage-only to a site-wide layout (currently mounted in `pages/index.vue` only). The title dead zone is homepage-specific and would need to become per-page config.
- B8. OPEN: scroll-coupling — the skin is `position: fixed`, so it does not scroll with the page. User raised wanting it to scroll; parked deliberately. Simulating the whole document height is linear in area and expensive; a bounded per-section canvas is the viable version.
- B1. RESOLVED 2026-07-29: not a hero element; the ACF portrait stays as the hero. RD becomes a full-viewport, semi-interactive texture layered over the paper-grid page background site-wide.
- B2. IDEATION (do before building): settle what "semi-interactive" means (cursor/scroll/click-seed/none), how it composites over the grid (blend mode / opacity / full-bleed vs masked bands), palette + density, and the mobile fallback. See conceptual Open Questions.
- B3. Static prototype first — a single full-bleed frame over the grid paper — to judge density/palette/compositing before any animation.
- B4. Then the cheap engineering: coarse low-resolution sim grid upscaled (never per-pixel full-res), 8–15 FPS, offscreen + featured-media-transition pause, reduced-motion static frame, a settled mobile fallback (full-viewport canvas is the classic phone-killer), and static-generation verified. Perf is the gating concern here because it is full-viewport.

### Thread C — Conway's Game of Life on the Side Projects card (FIRST SLICE)
**IMPLEMENTED 2026-07-30 — pending Human QA (see Ready For Human QA). C1–C9 done; C10 (static-gen verify) is part of QA. Steps kept below for reference.**
Section context: `HomeSideProjectsLink.vue` — the `.link` (a NuxtLink `<a>`) is `position: relative; overflow: hidden`, with a dark `--texture-terminal-scanline` background, white text, and `--color-terminal` (#218d4e) green accents; content is already `z-index: 1`. A phosphor-green GoL over that terminal ground is the intended look. Full technical design preserved below under "Reference: Conway technical design." Atomic steps:
- C1. `GameOfLifeBackground.vue`: `<canvas aria-hidden>` absolutely positioned (`inset: 0`) inside the Side Projects `.link`, `z-index: 0`, `pointer-events: none`. The link's `overflow: hidden` clips it.
- C2. Sim: `Uint8Array` grid + `next` double-buffer, toroidal neighbor counting, B3/S23 rules.
- C3. Loop: `setTimeout` step at ~8–12 FPS + `rAF` draw; draw only alive cells (`fillRect`, `cellSize - 1` for grid gaps).
- C4. IntersectionObserver (`rootMargin: 100px`) start/stop offscreen; **restart-fresh (re-randomize) on each viewport-enter** — the chosen lifecycle, which also solves GoL stagnation (never shows a settled/dead board).
- C5. ResizeObserver on the card to re-init grid dims on resize/font changes.
- C6. Reduced-motion: skip the loop under `reduce`, render one static initial frame (not blank).
- C7. Styling: cells in **terminal/dark green** (`--color-terminal` #218d4e family, resolved to a fixed rgba — `var()` won't resolve in canvas), **~20% opacity as a STARTING point — tune opacity and shade together over the dark scanline ground** (the 20% was picked under a mistaken "over cream" premise). Transparent canvas.
- C8. **Hover-inject**: `mousemove` on the `.link` element (not the canvas — it's `pointer-events: none`) seeds gliders/clusters near the pointer.
- C9. Pause on `useFeaturedMediaTransitionState().active` (in addition to offscreen pause).
- C10. Verify on static-generated output; watch for lazy-load/generation interactions (cf. the CDN loop-nav non-render bug logged in misc intake).
- OPEN: final green shade + opacity (paired); the mobile/touch hover-inject story (tap-to-seed vs non-interactive on touch).

### Thread D — Slit-slip motion expansion
**DONE 2026-08-05 — see Done. Remaining:**
- D3. OPEN (low priority): sweep for any further arrow CTAs beyond the four now converted. Nothing known outstanding; this is a "if you spot one" item, and the shared mixin makes each one a two-line change.

### Thread F — Button hover character
No work remains. The speculative accent-flash/fill idea was never selected and is dropped rather than forcing another motion treatment during closeout.

### Thread G — Featured-media transition frame-pacing audit
- G1. READY FOR HUMAN QA: reopened narrowly after 2026-08-31 reports of Latest Writing blinking and rougher local case-study flights. Writing-card transitions no longer wait through the case-study-only 500ms surroundings choreography after their source title/date hide. Homepage writing cards, writing archive rows, and case-study cards now warm the destination route component as well as CMS data; case-study viewport prefetch starts farther ahead. Removing the CPU margin organisms also prevents two high-cost simulations from waking shortly after a writing-detail handoff. The 2026-09-01 browser audit exercised homepage writing → detail, archive row → detail, a lower article → archive with preceding rows staged, and case study → detail → homepage; every path completed without stuck layers, hidden destinations, or console errors from the transition system. A fresh public static artifact then generated successfully and passed the static inspection plan with all generated asset references present and no local CMS/API references. A subsequent human report isolated one remaining archive → writing-detail title-ground blink. Git archaeology established that the TOC was only correlated in time: the TOC underlap refinement changed the article body matte but not the hero/title transition. The regression candidate was introduced earlier, after the last settled transition commit, when writing-detail arrival began animating the entire cream page surface from transparent. Several attempted handoff patches then changed multiple variables at once—instant clone removal, a writing-specific Vue leave path, and an explicit prepaint state—so they never tested the historical ownership model with only the regression removed. Removing the page fade and restoring one-state clone ownership still left a human-visible blink, establishing that the seated clone-to-header ground crossfade itself remained a perceptible ownership swap even without a literally transparent page frame. The current correction therefore makes the permanent destination header the owner of its cream ground as soon as the destination becomes visible. During the flight only its duplicate title/meta are hidden; the stationary ground remains painted underneath the opaque moving clone, so clone seating and removal can no longer expose the hero image or manufacture a new ground. The clone still carries its own captured opaque ground while moving, and the existing one-state leave crossfade remains for text/media continuity. Typecheck and lint pass (with only the two pre-existing `vue/no-v-html` warnings). Human QA should exercise `/writing` archive → detail at normal speed, ideally once cold and once warm, and confirm that the title ground is continuously present through seating and crossfade.

- G1 UPDATE 2026-09-02 — READY FOR HUMAN QA: the permanent-ground workaround above was not sufficient in human QA, so the transition now encodes the requested overlap model directly. Transition lifetime, clone geometry, and handoff ownership are separate state: after the clone's real flight `transitionend`, the coordinator keeps clone A fully opaque, waits boundedly for destination media B to decode, animates the shared destination media and text-ground surfaces to full opacity, waits for their actual named CSS animation completion, and then holds a double-`requestAnimationFrame` committed paint with A and B both opaque. Only afterward does `releasing-clone` start the two Vue leaves; `active` remains true until both teleported media/text layers report `after-leave`, with a bounded cleanup fallback. Reverse destination cards now reveal their titles, media treatments, and shared text plates during this B-enter phase rather than waiting for `active` to clear. Forward writing and case-study detail headers follow the same ownership phases. The accidental global `.site-main` isolation is now homepage-only, restoring the required interior stacking order of clone text above real header/content above clone media. The invariant is documented beside the state machine and completion code, in `FeaturedMediaTransitionLayer.vue`, in `AGENTS.md`, and in `docs/design-system.md` so future cleanup does not collapse the intentional overlap. The follow-up cleanup gave clone leave its own `--featured-media-clone-leave-duration` token instead of borrowing the separately meaningful duotone-fade name. Typecheck and lint pass (with only the two pre-existing `vue/no-v-html` warnings), and `git diff --check` passes. Instrumented local browser traces confirmed the ordering in both writing directions: B remained hidden while A was opaque; then B entered beneath still-opaque A; only after B reached opacity 1 did A's leave opacity begin decreasing; both clone layers cleaned up with no console error, and the interior main stacking context reported `isolation: auto`. Human QA confirmed that the archive → writing-detail blink is resolved; the other transition directions remain useful closeout spot checks.

## Ready For Human QA

- A26. Cursor follow-up: preserve the supplied icons' platform shadows and let the case-study image inherit its enclosing link's custom cursor instead of forcing the native hand. Verify graphic and textplate hover. The enlarged crops keep the complete shadow within the native 32px cursor canvas.
- A27. Organism lifecycle follow-up: keep document patches mounted while offscreen and pause their animation loops rather than destroying their contexts. Their baked initial seeds load through the same loader as the homepage; scrolling back resumes retained chemistry, not a new seed. Up to eight small contexts can remain allocated until leaving the article. Check continuity when revisiting a patch. This replaces A23's offscreen teardown behavior.
- A28. Possible browser-Back jank from writing detail to Home remains unconfirmed. The router suppresses saved-position restoration while a featured-media transition is marked active; no speculative timing or overlap change was made. Compare first and warmed Back trips with trails enabled/disabled before assigning a cause.

- A23. 2026-09-07: Replace the fixed margin field with document-positioned organisms beginning after 150vh. `ArticleMarginOrganisms.vue` reserves padded space around wide/full-width blocks, floats, figures, tables, code, quotes, and sidenotes; replans after resize, fonts, and image loading. Independent 330px-max canvases use the homepage GPU simulation with local pointer coordinates. Only near-viewport patches mount, and their contexts are released offscreen; returning patches restart from baked seeds. Browser QA on the footnote fixture confirmed safe placements, two absolute 330×420 canvases during scrolling, and no organisms at the top. Check visual density, smaller-canvas seed appearance, and placement on other articles. This supersedes the fixed-field interpretation recorded in A22 without deleting that history.
- A24. 2026-09-07: Keep ghost trails enabled and expose echo count (1–8), opacity, and spread in Motion QA. The panel now lives in the shared layout, retaining expansion and settings across client-side navigation on normal development routes; reload restores defaults. Echoes remain decorative and complete before the existing flight ends: they neither replace nor delay the destination-first ground overlap. Browser QA confirmed panel persistence Home → writing detail and no leftover echoes. Dial in preferred trail settings.
- A25. 2026-09-07: Preview the supplied cursor sprites through the Motion QA checkbox. Cropped the arrow, pointing hand, and I-beam into transparent 32px native cursor PNGs, omitting decorative pedestals and ambiguous generated symbols. Native fallbacks remain; fine pointers only and off by default. Browser QA confirmed the cursor toggle persists after navigation. Judge scale, hotspots, and character before adopting it. `corepack pnpm check` passed with only the two pre-existing About/Now `v-html` warnings.

<!-- Original QA request, kept for the record:
- **Slit-slip on the two homepage arrow CTAs (Thread D)** — implemented 2026-08-05. "More about me →" in the Vital Info box (`HomeVitalInfo.vue`) and "View writing archive →" under Selected Writing (`HomeLatestWritingSection.vue`) now do the horizontal slit-slip on hover/focus, matching the downward one on the file-download button and footnote more/less toggle. Check: (1) hovering either link sends the arrow out to the right and brings it back in from the left, with nothing spilling outside its slot; (2) it reads at the same speed and weight as the "↓ Download" arrow — they should feel like one family; (3) keyboard focus triggers it too, not just hover; (4) the underline sweep still runs alongside it; (5) the arrow is not read out by a screen reader (it is real markup now rather than CSS content, marked aria-hidden); (6) nothing shifts in layout when the animation runs; (7) reduced-motion shows no animation.
-->

## Done
- A29. Human-approved trail defaults: eight echoes, opacity 0.18, spread 0.15. Applied to fresh page loads; existing in-session adjustments remain user-controlled.
- A22. VERIFIED 2026-09-07: the margin placement and optional trail trial pass `corepack pnpm check` (only the existing About/Now `v-html` warnings). Local browser QA showed exposed organisms beside body prose and cleared floated quote surfaces, confirmed the hidden canvas class at scrollY 0, and completed Home → writing detail with the trail option enabled and no surviving clone/echo elements. Visual judgment of trails and final margin placement remains with the user.
- G2. DONE 2026-09-07: Original intake: "When testing on phone I noticed that navigating from home to a detail page, and then navigating back home animates the transition different than if you navigate from home to a detail page and then hit the browser \"back\" button. Hitting the back button looks more janky, not sure why." Human retesting now looks fine; close this report unless it recurs. The resolved intake is preserved here from `docs/scratch/misc.md`.
- A9. READY FOR HUMAN QA: Selected Work now combines the approved catalog shuffle, existing colour reveal, inset-image parallax, and baked halftone in production context. **Practice/discipline** and **engagement context** are explicit ACF fields on Case Study edit screens and GraphQL fields on the `CaseStudy` type. Empty fields use explicit “Practice / discipline” and “Engagement context” prompt copy so the missing editorial input remains visible rather than making the hover appear broken or inventing project facts. Pointer travel is capped at 28px with geometry-derived overscan, the outer transition hooks remain fixed, and touch activates a substantially centered card. The CMS-original comparison was removed after the baked halftone passed second-person review. Verify real authored metadata, prompt-copy behavior, mobile activation, and card-to-detail transition handoff. **CLOSED 2026-09-07:** human review confirmed settled animation decisions may be closed; preserve this entry's original history.
- A7. READY FOR HUMAN QA: the earlier heading-only negative-Z entrance and scroll-scrubbed departure felt isolated from their otherwise stable sections, so they were replaced by a real-homepage comparison of viewport choreography and seven pointer geometries. Human review selected **pointer proximity** and rejected broad viewport entry/pre-exit motion plus the other six heading models. The production field applies page-relative motion to Selected Work, Latest Writing, Testimonials, and Side Projects; samples stable untransformed letter wrappers to avoid feedback jitter; pauses during featured-media transitions; and keeps overflow visible to prevent glyph-box clipping near the pointer. The Selected Work accent is now excluded from the field so the lettering moves around a stable signal. Verify all words/lines respond, direct pointer overlap does not clip or chatter, tablet compositions remain unchanged, and reduced motion is static. **CLOSED 2026-09-07:** human review confirmed settled animation decisions may be closed; preserve this entry's original history.
- A16. READY FOR HUMAN QA: the broad occasional blue current and corrected symmetric rotating star are accepted and locked on for Selected Work cards. The travelling wavy current, breathing brackets, orbiting dots, and rotating dial are rejected and removed from both Motion QA and production code. Reduced motion preserves the star as static punctuation rather than removing it. Latest Writing's extremely low-travel card proximity and rotating crosshair are accepted and locked on; the strength control remains for tuning. The footer quiet signal and CMS-heading ticker are also accepted and locked on. Motion QA now exposes decisions still requiring comparison rather than toggles for settled behavior. **CLOSED 2026-09-07:** human review confirmed settled animation decisions may be closed; preserve this entry's original history.
- A17. READY FOR HUMAN QA: a self-hosted pixel-art construction GIF now appears in a fixed yellow-and-black “Always under construction / Always improving” banner on the homepage only. It is enabled by default, has a Motion QA toggle during development, sits at the top-left, compacts on phone, can be dismissed immediately with a real button, automatically dismisses after roughly 20% of a viewport of actual scrolling (capped at 180px), and is removed from print. The scroll behavior has been browser-verified. It began as temporary launch chrome, but human review found it charming enough to retain as homepage character rather than site-wide status chrome. **CLOSED 2026-09-07:** human review confirmed settled animation decisions may be closed; preserve this entry's original history.
- A18. READY FOR HUMAN QA: the homepage BLUF pill now carries a continuously rotating slightly elliptical star, without a satellite ornament, and is an explicit button disclosure control. Opening it explains “Bottom Line Up Front” as clarity-first, depth-on-demand working practice and provides jump links to Selected Work, Collaborators, Side Projects, and Latest Writing. The Testimonials section now exposes the `collaborators` anchor. Its phone layer now matches the desktop disclosure layer so the panel paints above the bespoke hero composition rather than opening invisibly behind it. Verify the copy, disclosure placement across the bespoke desktop/tablet/phone hero compositions, keyboard behavior, and whether the jump menu is useful enough to retain. **CLOSED 2026-09-07:** human review confirmed settled animation decisions may be closed; preserve this entry's original history.
- A15. BRAINSTORM: make the Selected Work accent intentional through repetition rather than defending it as a one-off. Candidate family placements are testimonial signal bars, article metadata punctuation, and occasional section eyebrows where a real structural accent already belongs. Repetition should share material behavior, scale logic, or pointer response without requiring several WebGL canvases or adding marks to surfaces that do not need punctuation. **CLOSED 2026-09-07:** human review confirmed settled animation decisions may be closed; preserve this entry's original history.
- A8. REJECTED 2026-08-31: article margin organisms are removed from writing-detail pages. The specimen was a separate 2D/CPU Gray-Scott implementation, not a masked view into the homepage WebGL ecology. At two 240×368 fields, two simulation passes every 38–42ms meant roughly nine million JavaScript cell updates per second while still advancing the chemistry about seventeen times more slowly than the homepage's GPU field. Baked seeds made the opening resemble the homepage, but different equations, terrain, transport, thresholds, and cadence made the resemblance disappear in motion; increasing speed would worsen page and transition frame pacing. Revisit only as one shared GPU renderer with measured reveal windows, not as multiple independent canvases. **CLOSED 2026-09-07:** human review confirmed settled animation decisions may be closed; preserve this entry's original history.
- A0. IMPLEMENTED / HISTORICAL LAB RECORD: the broad `/dev/motion` comparison phase is complete. It began as a fake-content surface, moved to production compositions and CMS content when abstraction obscured judgment, and ultimately selected baked halftone, catalogue metadata, inset image parallax, colour reveal, pointer-proximity headings, writing-row slit entrances, and a set of quiet section signals. It rejected moving partitions, print registration, aperture reveal, project signal, atlas glyph, broad viewport entry/exit, testimonial reveal, CMS-original imagery, and CPU margin organisms. The remaining controls now represent genuine finalists or tuning parameters; accepted behavior has moved out of the lab and into production context. See A7, A9, A13–A19, and G1 for current QA boundaries. **CLOSED 2026-09-07:** human review confirmed settled animation decisions may be closed; preserve this entry's original history.
- **B12 — Phone preview without static generation/CDN deploy.** The self-signed LAN preview was the wrong foundation: it produced an unreliable phone origin, the reported Android visit received placeholder/error CMS state even though later host requests succeeded, and it did not deliver usable orientation input. `corepack pnpm start:phone` now waits until the public CMS GraphQL endpoint responds, then starts Nuxt through its accountless Cloudflare Quick Tunnel integration; `corepack pnpm start:frontend:phone` starts only the tunneled frontend when the CMS is already running. No Cloudflare account or stored secret is required. The random trusted HTTPS URL and QR code change each session and are publicly reachable only while the command runs. WordPress queries and uploads remain behind the restricted same-origin Nuxt bridge. On Android, open the printed URL, confirm the real CMS portrait, testimonials, case studies, and writing load, confirm the homepage chip reaches **Tilt QA: active**, and confirm `/dev/rd` reports changing numeric orientation values. The first tunnel launch on this machine exposed an old bundled `cloudflared` auto-update failure; pinning the helper to download `latest` prevents that first-run teardown. **DONE 2026-09-07:** the QR/tunnel workflow and mobile tilt were confirmed through the subsequent phone QA; this original setup request is complete.
- A14. READY FOR HUMAN QA: testimonials no longer animate in or out. The title uses pointer proximity and remains one line in the desktop/tablet side column. The CMS-selected texture now responds to the pointer's viewport position even while the pointer is outside the section; touch devices retain calibrated projected-orientation control. Cards have more internal whitespace. The quotation signal is a closing mark anchored in the bottom-right corner, underneath the content layer but outside the quote body, so it remains typographically present without interrupting reading; after the previous correction pushed it too far right, its glyph box is now pulled left by roughly 0.38em. Its slow blue wash remains stronger. The accepted travelling striped block accent remains visible alongside the quotation wash rather than being substituted away. Reduced motion freezes texture, bars, quote wash, and heading field. Verify the new corner placement across short/long testimonials and the global pointer mapping. **DONE 2026-09-01:** human QA confirmed the corrected quotation placement as good enough, completing the remaining testimonial card judgment.
- A19. READY FOR HUMAN QA: Latest Writing's card proximity is enabled by default, preserves card internals and transition hooks, clears during featured-media transitions, ignores touch, and becomes static under reduced motion. The first persistent candidate, an edge-relay pulse across the top border, was rejected. Motion QA now offers a mutually exclusive dropdown with five other ambient families plus None: **corner constellation** (three offset registration dots), **date beacon** (a pulse attached to metadata), **image blue tide** (a slow chromatic wash confined to media), **registration drift** (opposed corner marks), and **border breathing** (slow blue border/shadow modulation). They share one speed control, stagger by card, stop offscreen and during featured-media transitions, and disappear or freeze under reduced motion. Side Projects applies the selected heading pointer field to its CMS-backed large linked title. Select or reject the bento ambient family, verify the accepted low pointer travel stays quiet enough, and verify Side Projects wrapping on desktop, tablet, and phone after authoring CMS copy. **DONE 2026-09-01:** human review rejected all five follow-up ambient families as either illegible or unattractive. Their markup, animation CSS, state, and controls were removed. Latest Writing intentionally keeps only the already-accepted low-travel card proximity and rotating crosshair rather than forcing one autonomous effect per card.
- F1. Explore a more graphic button-hover (accent flash on entry / fill animation) vs the current darken/opacity. OPEN: which treatment. **DROPPED 2026-09-01:** this was an early speculative candidate, never an approved implementation target. Existing button behavior is sufficient for animation-spike closeout.
- A13. READY FOR HUMAN QA: the Selected Work production rule is narrowed to three serious candidates: SVG irregular flag, WebGL shedding lava, and a WebGL hybrid that keeps the flag and its trailing fragments in one renderer. Wide-desktop flag defaults remain 24 fluidity, 2.65× speed, 234/−23 offset, 474×8 viewport, 4.6× thickness, 8× amplitude, and 1.7× frequency. Compact desktop from 1400–1891px keeps the reviewed 100/−23, 474×8 geometry; a separate narrow-desktop state from 1200–1399px uses the later reviewed 57/−22, 362×8 geometry with 6.8× amplitude, preventing the rule from underlapping the portrait at the bottom of the desktop composition. Tablet and phone retain their approved independent defaults: tablet 71/−11, 206×8, 4.7× amplitude; phone −2/−23, 250×8, 5.6× amplitude. Standalone lava keeps its separate reviewed preset—4.25× speed, 279/0, 506×32 on wide desktop, 1× height, 0.85× body length, 1.6× dispersion, and 1× reach—so switching textures does not destroy the flag's geometry. The first clipping repair was rejected because shifting every body lobe inward and coupling dispersion to core movement produced an unwanted vertical bob while an aggressive boundary fade suppressed shedding. The revision restores the earlier body placement, limits dispersion to shed material, calms vertical lobe travel, terminates the left edge by subtracting from the scalar field rather than fading the rendered result, and gives emitted pieces more room to remain visible before their edge fade. The first hybrid used independent ellipses and read as simple particles; the next used explicit SVG tendrils and read as silly antennae or applied decoration. The first implicit-field replacement emitted orthogonally and merged large blobs into the wave, making it resemble a snake that swallowed something. A subsequent tail-only correction kept the wave clean but concentrated permanent-looking bubbles at its endpoint. The current pass restores shedding sources across the full wave, lets each emerging mass briefly inherit its local surface, then freezes its release-time origin so detached material no longer wags with the live ribbon. Independent blobs travel right with varied reach across the extended 165% transparent canvas. Late in each lifecycle, the path returns the mass to a point farther along the live ribbon and resumes current wave motion as the fields overlap. The first recapture pass shrank and faded the mass at the same time, making that inheritance technically present but visually imperceptible. Recapture now finishes earlier, the returned lobe remains visibly attached and wave-driven for a sustained phase, and a separate later absorption contracts it into the body. Density and whip-force controls remain. The mark remains independent of pointer proximity. All modes pause offscreen, during featured-media transitions, and under reduced motion. Final choice remains irregular flag versus repaired lava versus the distributed-shedding hybrid. **DONE 2026-09-01:** human review selected the irregular SVG flag without shedding. The standalone lava and hybrid experiments were removed from production, Motion QA, responsive settings, and the component tree; their iteration history remains here because it explains why the simpler flag won.
- [x] A6. READY FOR HUMAN QA: homepage testimonials perform the approved 140ms-staggered lateral wipe when their grid enters the inner viewport and reverse the wipe while roughly the final 16% of the grid is still visible; neither direction is scrubbed to scroll position. The title and Collaborators eyebrow now use the same explicit lateral slit instead of relying on a subtle wrapper fade. Vertical viewport choreography now follows a directional pair: while scrolling down, content enters bottom-to-top and exits top-to-bottom; while scrolling up, it enters top-to-bottom and exits bottom-to-top. Hidden elements may switch their collapsed edge while fully offscreen when scroll direction reverses, avoiding an onscreen snap while preserving the correct next entrance. The broader viewport-motion experiment can disable entry motion, and a separate exit checkbox can leave entered content in place. Verify title/card timing in both scroll directions and reduced motion. **DROPPED 2026-08-20 after second-person review:** the reveal made isolated groups appear against an otherwise stable page and was judged less convincing than persistent ambient/input-driven motion. The testimonial lateral wipe and the broader homepage viewport-entry/pre-exit system were removed rather than shipped.
- [x] **B11. Finalize mobile reaction-diffusion tilt behavior.** APPROVED ON ANDROID 2026-08-12. Mobile tilt now drives a restrained composite of terrain-steered coral-front transport (`10.5` cells/sec), quiet fertility drift (`0.028`), immediate display translation (`0.055`), and trace hospitality rather than accelerating or deforming the whole field. Android autonomous drift is off once orientation input is active; permission-gated devices retain a quiet fallback. Tilt is mapped to capped velocity, so a held lean continues moving without runaway acceleration or tiger-striping. Tilt blossoms and stripe-prone anisotropy/advection/rate deformation remain off. The scroll-only reset was browser-chrome resize jitter: height-only touch resizes now preserve simulation textures, and display phase accumulates in UV space so toolbar changes cannot snap earlier motion. `/dev/rd` retains the independent tuning controls and phone QA readout.
- [x] **B10. OPEN: phone reads as dead.** PARTLY ADDRESSED 2026-08-05 — the ambient drift had been lowered globally when it should only have come down on touch; desktop is back to 0.27 and touch stays at 0.085. Whether the phone still reads as inert at that value is the part that needs first-hand testing. Original note: Second-hand report 2026-08-05 (not yet reproduced first-hand — needs testing on a real device before acting). On a phone the effect may be too inert: no cursor, and if tilt is unavailable or the device is held still, the only motion left is the ambient drift at 0.085 — which was tuned down for desktop where the cursor supplies the liveliness. Candidate fixes: a higher ambient drift on touch devices specifically, or a stronger resting wander. Do not tune this from the desktop; it needs eyes on a phone. — SUPERSEDED 2026-08-11 by the opposite first-hand report: mobile tilt was too aggressive and produced strobing tiger stripes. B11 replaces this framing with independent touch ambient, tilt translation, and tilt reaction controls.
- [x] **E1. Animate the accordion toggle +/− with a brief rotation instead of a static swap.** Already present before this spike: the shared accordion recipe swaps +/− and rotates the glyph 180° over 220ms. No new implementation slice was needed.
- [x] **Thread C — Conway's Game of Life on the Side Projects card.** Approved 2026-07-30. `HomeGameOfLifeBackground.vue`, terminal-green over the scanline ground, `CELL_SIZE = 8`, opacity 0.2, restart-fresh on viewport-enter, persistent radial hover-inject. Open follow-up: the touch story (no `mousemove` on mobile).
- [x] **D1. Extract/confirm the reusable slit-slip recipe from existing callsites (footnote/file/lightbox) so it can be applied consistently (preserve the clipped-slot).** — DONE 2026-08-05. Lives in `packages/styles/shared-components/_slit-slip.scss`, forwarded to every component style block. The single entry point is `@include arrow-cta-slit('.your-link')`, which emits the clipped slot, the glyph, the hover/focus slip and the reduced-motion opt-out together, so callsites cannot drift apart. Two non-obvious constraints are recorded in that file: the keyframes live *inside* a mixin because the file is injected into every component and would otherwise emit a copy everywhere; and the arrow's font is **pinned to sans rather than inherited**, because these CTAs do not share a face (one italic, one uppercase mono) and an inherited arrow rendered as visibly different glyphs on links meant to read as the same control.
- [x] **D2. Apply to: file-download arrow, homepage "More about me" CTA, "View Writing Archive" CTA, and other arrow CTAs.** — DONE 2026-08-05. Converted: `HomeVitalInfo.vue` ("More about me"), `HomeLatestWritingSection.vue` ("View writing archive"), and `pages/now.vue`, which had a third undiscovered copy of the same pseudo-element pattern. The file-download and footnote arrows were already correct and are the reference implementation; PhotoSwipe prev/next was already done.
  - **Why these were never converted before:** they drew the arrow with `::after`. A pseudo-element cannot clip its own text — the text and the box are one element, so transforming it moves the clip along with it — so they could only nudge the arrow a few pixels. The slit needs real markup: a clipping slot wrapping a glyph that moves independently. This is the thing to check first if a future arrow "won't animate".
  - Shipped with a bug that human QA caught: the archive link rendered **two** arrows and animated neither, because the `::after` removal silently failed to apply while the new markup was added. Fixed in the same session. The lesson is in the durable list below.
- [x] **B5. Mobile motion story.** There is no pointer on touch, so the cursor attraction was desktop-only. — DONE 2026-08-05, REVISED 2026-08-11. iOS is settled by decision: `DeviceOrientation` needs a modal permission prompt, far too much friction for a background texture, so it is never requested — iOS runs on touch plus a slow wander, Android additionally gets tilt. Two framings were rejected: a **rolling influence point** (the Jackalope precedent), which works and is clearly visible in the harness but gets lost in the homepage's deliberately fragmented coral; and **tilt-as-acceleration**, which integrates and therefore ran away, pinning at maximum and strobing. A bounded position map fixed the runaway but stopped once it reached its destination. The revised model maps tilt directly to **bounded velocity**: a held lean keeps moving at one capped speed without accumulating acceleration. Direction comes from grading the fertility threshold toward the tilt. Lessons 7–11 below all come from this item.
- [x] **Thread B — Reaction-diffusion page-wide skin.** Approved 2026-08-03. `HomeReactionDiffusionBackground.vue` — a fixed full-viewport WebGL2 canvas (`z-index: -1`, `pointer-events: none`) over the paper grid, below content, with the footer given its own positive `z-index` so it paints over it.

  **Final shape.** GPU Gray-Scott, ping-pong `RGBA16F` textures, ~15 sim passes per frame. `F 0.0496 / k 0.0619`, diffusion `0.32/0.16` at `dt 0.6` (higher than textbook so the Turing wavelength spans enough cells to read as smooth curves rather than blocky ones). Negative space from a drifting value-noise **fertility mask**; turnover from that drift plus **discrete nucleation blobs**. Cursor **lowers the local kill rate** so coral grows toward the pointer, broken up by a moving speckle field so it does not read as a painted disc. A soft elliptical **dead zone** measured from the hero script spans keeps the wordmark readable. Resize copies live state into the new textures instead of reseeding. Warm-up develops a mature pattern in chunks behind a fade-in, so the page never opens on scattered dots.

  **Durable lessons — these cost a week of wrong diagnoses. Do not relearn them.**
  1. **Simulation state must be sampled `NEAREST`.** With `LINEAR`, a `vUv` landing a hair off a texel centre makes every neighbour read a blend of two texels — a sub-texel smear applied ~1000×/sec, i.e. advection. Use a separate sampler object so the display can still upscale `LINEAR`.
  2. **Rates belong per second, not per sim step.** Decay and drift constants applied per step are silently multiplied by the iteration count; changing "speed" then changes the look. Scale by the wall-clock time each step represents.
  3. **Never seed with a per-cell hash plus a sliding offset.** `hash(cell + t)` with `t` advancing one cell per step means each firing point *draws a diagonal line*. That was the long-running "wind-blown" bug, it is inherent to the mechanism, and no tuning removes it. Use discrete blobs at random positions instead.
  4. **Growth only spreads from existing pattern**, so drift faster than the growth front strands the field in barren land and it collapses. Nucleation is what raises that ceiling, because growth can then start *ahead* of the front.
  5. **`display: contents` elements have no box** — `getBoundingClientRect()` returns zeros. Measuring `.hero-title` silently disabled the dead zone for several rounds. Measure the children that actually have layout.
  6. **`clientY` grows downward; WebGL `uv.y` grows upward.** Flip when mapping pointer or element rects into the sim, or effects land mirrored.
  7. **Tilt input is a bounded POSITION map, never an acceleration.** Acceleration integrates, so a sustained lean runs away and a phone held still keeps moving ever faster. Reference implementation: `temp-ref-assets/Jackalope/assets/src/js/customscripts/parallax.js` (Wagerfield) — note that `rellax.js` beside it is scroll-only and has no orientation code. Its calibration threshold, which re-zeroes when input strays too far, is what stops a new resting attitude reading as a permanent tilt.
  8. **Read tilt from gravity projected onto the screen plane, not raw beta/gamma.** `gamma` is ill-conditioned near vertical: at `beta ≈ 90°` it swings wildly on the smallest movement. Scaling it by `cos(beta)` takes its influence to zero exactly where it destabilises — a ±15° wobble moved the input by 1.071 in the raw model at any attitude, and by 0.009 at 89° after.
  9. **Bounding how FAR is not bounding how FAST.** A bounded offset still let a sharp tilt drive the pattern at 2.5× the ambient drift, and fast lateral movement shears coral into stripes. A slew limit on offset velocity is what actually keeps it calm. Responsiveness is then set by that cap and the excursion *together* — a reversal must cross twice the offset at the capped speed.
  10. **Check which term actually dominates before tuning.** Hours went into the tilt cap while the ambient drift, eighteen times larger, was the real source of the speed being tuned against. Compare the magnitudes first.
  11. **Directional effects that bias an axis will stripe.** Anisotropic diffusion stretches the Turing wavelength; a reaction-rate gradient matures one side faster. Both read as bands. Grading the *fertility threshold* instead gives a direction without one, because it only changes where coral may live, not how it grows.
  12. **Verify each edit landed, not just the first one.** A find-and-replace across two similar components silently matched only one, shipping a link with two arrows. Assert that every replacement actually changed the file, and grep the *whole* app for the old pattern rather than confirming the file you happened to look at.
  13. **A step that must be remembered, whose omission is invisible, is a broken design.** The baked opening states were first wired through a hand-edited manifest; five baked files silently never loaded because the manifest was left empty, and the procedural fallback looks similar enough to pass for success. Replaced with a build-time glob over the folder, plus a dev-only console line reporting when a baked seed is actually used.
  14. **Process:** the breakthrough came from building `/dev/rd` — a harness that made the reaction, mask, drift and seeding independently toggleable with raw-field views. Debugging four coupled subsystems through one composited output produced confident, repeatedly wrong theories. **Build the isolation harness early.** It is kept (not deleted) as a possible easter egg / blog toy.
- [x] Thread G — Featured-media transition frame-pacing (jank) audit. **Dropped 2026-07-29** without work: Human QA reports the previously-janky case-study/writing card→detail morph has effectively cleared up and looks good now, so no audit is warranted. The original investigation plan (reproduce case-study vs writing forward/reverse × cold/warm cache × desktop/phone; performance trace over the ~600ms flight; isolate filter/layer vs geometry cost; prototype transform-first/FLIP geometry) is preserved in the conceptual doc; reopen only if the jank regresses.

---

## Reference: Conway technical design (preserved from retired scratch)
### Grid + step (B3/S23, toroidal)
Flat `Uint8Array` (0=dead, 1=alive), indexed `row * cols + col`. Double-buffer (`grid`, `next`) to avoid read/write conflicts and GC churn.

```ts
function step() {
  for (let r = 0; r < rows; r++) {
    for (let c = 0; c < cols; c++) {
      const neighbors = countNeighbors(r, c);
      const alive = grid[r * cols + c];
      next[r * cols + c] =
        alive ? (neighbors === 2 || neighbors === 3 ? 1 : 0)
               : (neighbors === 3 ? 1 : 0);
    }
  }
  [grid, next] = [next, grid];
}

function countNeighbors(r: number, c: number): number {
  let count = 0;
  for (let dr = -1; dr <= 1; dr++) {
    for (let dc = -1; dc <= 1; dc++) {
      if (dr === 0 && dc === 0) continue;
      const nr = (r + dr + rows) % rows;   // toroidal wrap
      const nc = (c + dc + cols) % cols;
      count += grid[nr * cols + nc];
    }
  }
  return count;
}
```

### Low-FPS loop (step on setTimeout, draw on rAF)
```ts
function startLoop() {
  const FPS = 10;
  function tick() {
    step();
    requestAnimationFrame(draw);
    stepTimerId = window.setTimeout(tick, 1000 / FPS);
  }
  tick();
}
```

### Draw (alive cells only, grid gap via cellSize - 1)
```ts
function draw() {
  ctx.clearRect(0, 0, canvas.width, canvas.height);
  ctx.fillStyle = /* resolved signal blue rgba; var() may not resolve in canvas */;
  for (let r = 0; r < rows; r++) {
    for (let c = 0; c < cols; c++) {
      if (grid[r * cols + c]) {
        ctx.fillRect(c * cellSize, r * cellSize, cellSize - 1, cellSize - 1);
      }
    }
  }
}
```

### Offscreen pause + resize
```ts
const observer = new IntersectionObserver(
  ([entry]) => { entry.isIntersecting ? startLoop() : stopLoop(); },
  { rootMargin: '100px' }
);

const resizeObserver = new ResizeObserver(() => {
  const { width, height } = canvas.value!.getBoundingClientRect();
  canvas.value!.width = width;  canvas.value!.height = height;
  cols = Math.floor(width / cellSize);  rows = Math.floor(height / cellSize);
  grid = new Uint8Array(rows * cols);   next = new Uint8Array(rows * cols);
  randomize(); // ~25–30% initial density
});
```

### Reduced motion + styling
```ts
const motionOK = window.matchMedia('(prefers-reduced-motion: no-preference)').matches;
if (!motionOK) { drawStaticInitialFrame(); return; } // static frame, no loop
```
```scss
.gol-canvas {
  position: absolute; inset: 0; width: 100%; height: 100%;
  pointer-events: none;
  opacity: 0.2; // STARTING point over the dark terminal ground; tune with the shade
}
```
Resolved params: cell size 4–6px; ~8–12 FPS; ~25–30% initial density; toroidal wrap; **terminal/dark-green cells (`--color-terminal` #218d4e family, fixed rgba) over the section's dark `--texture-terminal-scanline` ground** (NOT cream); transparent canvas; **restart-fresh on each viewport-enter**; **hover-inject near the pointer** (track `mousemove` on the `.link`, not the canvas). Must also pause on `useFeaturedMediaTransitionState().active`.

## Reference: reduced-motion patterns (from retired scratch)
Prefer the affirmative opt-in so motion is off by default:
```scss
@media (prefers-reduced-motion: no-preference) {
  .animated-surface { animation: drift 6s linear infinite; }
}
```

## Reference: ambient CSS snippets (from retired scratch)
Testimonials drift:
```css
@keyframes drift { to { background-position: 20px 20px; } }
.inner { animation: drift 6s linear infinite; }
```
Grain shimmer:
```css
@keyframes grain {
  0%,100% { transform: translate(0,0); }
  10% { transform: translate(-2%,-3%); } 30% { transform: translate(3%,2%); }
  60% { transform: translate(-1%,4%); }  80% { transform: translate(2%,-2%); }
}
.grain-overlay { animation: grain 0.8s steps(1) infinite; }
```
Accent-rule pulse:
```css
@keyframes pulse-width { 0%,100% { opacity: 1; } 50% { opacity: 0.5; } }
.section-label::before { animation: pulse-width 4s ease-in-out infinite; }
```
