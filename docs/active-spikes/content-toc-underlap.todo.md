# Content, TOC, and Underlap To-Do
## Background
Promoted 2026-07-30 from `docs/scratch/content-toc-underlap.md` after a limited misc-inbox routing pass. Conceptual doc: [content-toc-underlap.md](content-toc-underlap.md). The spike groups detail-content regressions connected to the desktop article TOC, the cream `underlap-matte` treatment introduced to quiet content crossing the TOC rail, and contexts where that treatment is inappropriate.

## Project Organization
- Conceptual doc: `docs/active-spikes/content-toc-underlap.md`
- Operational doc: `docs/active-spikes/content-toc-underlap.todo.md`
- Promoted scratch source: `docs/scratch/content-toc-underlap.md` (deleted during promotion)
- Related durable docs: `AGENTS.md` → Gutenberg rendering rules; `docs/design-system.md` → editorial content rendering; `docs/visual-design.md` → article-body and TOC surface notes
- Historical context: `docs/archive/table-of-contents.md` / `.todo.md`, `docs/archive/content-blocks.md` / `.todo.md`, and `docs/archive/editor-polish.md` / `.todo.md`

## General Principles
- Treat underlap clearing as contextual TOC apparatus, not an intrinsic visual ingredient of every wide/full block.
- Preserve each block's own background, border, and hard shadow.
- Verify suspected shared causes before changing the shared mixin.
- Keep the TOC below authored content and make its auto-hide behavior conservative and geometry-based.
- Do not emit frontend-only TOC-clearing material in the WordPress editor.
- Do not paint cream into non-cream page contexts such as Side Projects.
- Prefer the smallest boring fix that makes the layering model explicit and repeatable.

## Current State Overview
- `packages/styles/_mixins.scss` defines `underlap-matte` as an oversized cream `::before` pseudo-element with negative z-index inside an isolated stacking context.
- Several shared-component recipes apply the matte to wide/full content, including File and Accordion.
- The File block has been observed losing its off-white surface and signal-blue left bar at wide alignment.
- Accordion interiors were observed with the wrong surface, although that specific background symptom may already be fixed; Accordion hard shadows remain reported as clipped or absent, especially when wide.
- The cream matte has appeared in the WordPress editor even though the editor has no frontend TOC.
- Side Projects uses a non-cream page context, so a cream clearing halo is visually wrong there.
- `ArticleToc.vue` currently auto-hides after at least three qualifying overlaps; it does not account for one object covering a large fraction of the expanded TOC.
- The desktop TOC should move roughly another 25px left to reduce routine contact with the wider content rail.

## Key File Pointers
- `packages/styles/_mixins.scss` — `underlap-matte` and width-alignment helpers.
- `packages/styles/shared-components/_file-block.scss` — File surface, left rule, alignment, and matte callsites.
- `packages/styles/shared-components/_accordion-block.scss` — Accordion surface, hard shadow, alignment, and matte callsites.
- `packages/styles/context-role/_wp-editor.scss` — editor adapters and shared-recipe consumption.
- `apps/frontend/components/content/ArticleToc.vue` — desktop rail geometry and overlap-count auto-hide heuristic.
- `apps/frontend/components/content/blocks/FileBlock.vue` and `AccordionBlock.vue` — frontend roots consuming the shared recipes.
- `apps/frontend/pages/side-projects/index.vue` — non-cream contextual QA surface.

## To Do
### Phase 4 — Validation And Closeout
- P12. Verify representative writing, case-study, Side Projects, and editor surfaces across default/wide/full blocks, then prepare the visual QA checklist below.

## Ready For Human QA
- P32. The TOC collapses on scroll too quickly, it needs a little more action before it collapses. Also it never collapses on scroll up, but it should probably have roughly the same collapse before for scrolling up or down. I'd say probably should be able to scroll 100vh before it collapses idk. Result: one viewport and then `150vh` both felt too short in successive human QA passes, so the desktop rail now auto-collapses after `200vh` of accumulated scroll travel once it has become fully visible, counting upward and downward movement equally. TOC-link programmatic scrolling remains suppressed, and manually reopening the rail resets the full travel allowance. Human QA should judge the revised `200vh` threshold on both a short and tall desktop viewport.
- P30. Restrict the full-width Image lightbox pointer target and focus indication to the visible height-capped image even though its semantic button remains a viewport-wide layout child. Preserve native button keyboard activation and image lightbox behavior. Result: full-width lightbox buttons ignore pointer events in their transparent wings while the child image restores pointer targeting and receives the button's visible keyboard-focus outline. Initial browser hit-testing showed that a wing point still resolved to the noninteractive but pointer-blocking figure, so the full-width figure shell was also made pointer-transparent while its rendered image and caption explicitly retain pointer interaction; this lets wing hits reach underlying content while image hits remain inside the native button.
- P31. Replace the 8px scroll release for an initially obscured TOC with an overlap latch: once hidden during settled entry, keep it hidden until no meaningful painted-surface overlap remains, then return to the accepted tolerant scrolling heuristic. Result: the initial decision now latches only when its settled pass finds a meaningful overlap and releases only on a later zero-overlap measurement; route changes reset the decision. Browser verification confirmed the Maps TOC stayed hidden after a 24px scroll and returned only after its obstacle cleared, already collapsed by the ordinary scroll behavior rather than flashing expanded first.
- P28. Make full-width Image matte painting and TOC obstacle measurement follow the actual height-capped image rectangle instead of the viewport-wide figure/button box. Preserve full-width Gutenberg layout and caption placement while removing unnecessary cream wings. Result: the viewport-wide figure remains the Gutenberg layout shell, while the rendered `img` now owns both the 25px cream ring and matching TOC obstacle outset; the transparent figure and full-width lightbox button no longer inflate collision geometry. Local browser inspection on the footnote QA route measured the figure/button at 1920px wide and the rendered matte-bearing obstacle at the actual 1024px image width.
- P26. Move the desktop TOC another 25px left at wide viewport sizes while preserving the viewport clamp and 1180px cutoff behavior. Result: the desktop offset moved from 120px to 145px while retaining the existing `max(var(--space-5), …)` clamp.
- P27. Remove the remaining cream-colored ordinary component surfaces from Pullquote and Media+Text in the WordPress editor without changing their public frontend styling. These are not underlap mattes—the editor already disables matte generation—but transparent editor-only overrides keep the no-cream authoring context consistent. Result: the editor Pullquote shell and Media+Text copy area now override their shared warmer-surface backgrounds to transparent while retaining the rest of their editor recipes.
- Confirm a representative writing and case-study detail page preserves the File surface and blue left rule plus the Accordion surface, border, and hard shadow at default/wide/full alignments.
- Confirm Side Projects shows no cream clearing halo around wide/full blocks and the WordPress editor shows normal block styling without frontend TOC mattes.
- Confirm the desktop TOC sits about 100px farther left than the original position, remains usable near the 1180px cutoff, is hidden on settled page entry by any meaningful painted-surface overlap, and still uses the three-intrusion/40%-coverage heuristic after the reader begins scrolling.

## Done
- P33. The scroll jacking animation to a location when you click the TOC on mobile doesn't work. It takes you near the proper location but not quite fully the right location. Result: the old click order started `scrollIntoView()` before closing the in-flow mobile TOC, so the list's collapsing height moved the heading away from the position already targeted by the smooth scroll. Selection now closes the list, waits for its actual CSS transitions to settle, and only then starts heading navigation. Reduced motion naturally produces no transition wait. Human QA confirmed mobile TOC links now land correctly.
- P29. Use a stricter TOC visibility rule during settled page entry: any meaningful painted-surface overlap hides the rail before reveal. After genuine user scrolling begins, retain the accepted three-intrusion/40%-coverage heuristic so small temporary crossings remain tolerable. Result: the settled entry pass hides for the first overlap that clears the existing 24px-by-18px meaningful-intrusion floor, then an 8px scroll delta releases visibility evaluation to the existing tolerant heuristic; route changes restore strict entry behavior. Local browser inspection on the Maps post confirmed the settled initial rail was obscured at scroll zero and became visible under the normal heuristic after a 24px scroll. Human QA found the 8px release too permissive because the still-overlapping TOC reappeared and immediately entered its scroll-collapse behavior; P31 supersedes that release rule with a zero-overlap latch.
- P25. Move the desktop TOC another 25px left at wide viewport sizes while preserving the viewport clamp and 1180px cutoff behavior. Result: the desktop offset moved from 95px to 120px while retaining the existing `max(var(--space-5), …)` clamp. Human QA requested one further 25px adjustment, continued in P26.
- P3. Check the same blocks in the WordPress editor and on Side Projects to establish exactly where cream matte output appears outside the desktop article-TOC relationship. Confirm visually that both contexts now omit the cream clearing. Human QA confirmed File and Accordion have no cream matte in the WordPress editor; earlier QA confirmed Side Projects columns and floated images omit the article matte without regressions. Pullquote and Media+Text retain separate ordinary cream component surfaces, addressed as an editor-consistency refinement in P27 rather than as leaked matte output.
- P24. Preserve the normal-width Accordion's bottom hard shadow when it is immediately followed by a wide/full matte-bearing block. Reserve enough layout clearance for the shadow without changing global matte geometry, TOC obstacle measurement, or sibling stacking order. Result: the Accordion recipe now reserves an additional `0.5rem` below its normal `space-5` gap only when it is a direct child of a frontend content flow with active underlap mattes, enough for the `0.35rem` hard shadow to finish before a following 25px matte begins without changing editor or non-article spacing. Human QA confirmed the bottom shadow is visible.
- P1. Reproduce the current default/wide/full File and Accordion rendering on representative writing and case-study detail routes. Record which reported symptoms still exist before changing styles: File surface, File left rule, Accordion interior, and Accordion shadow. Partial human QA confirms the wide File surface and Accordion interior are fixed; remaining alignment/shadow states are still pending. Final human QA confirmed the remaining normal-width Accordion shadow fix.
- P2. Inspect the rendered DOM, computed styles, and stacking/paint order for each reproduction. Determine whether the oversized negative-z `::before` matte is painting over the block's own background/border/shadow, whether clipping comes from a wrapper, or whether different symptoms have different causes. Source and generated-CSS inspection established the stacking-context cause; rendered computed-style confirmation remains pending. Subsequent targeted reproduction and human QA confirmed the resulting paint-order and shadow-clearance behavior.
- P23. Keep the TOC geometry pending while the article content or a layout ancestor is moving through its entry animation, then run the authoritative visibility pass when that motion finishes. Do not change the accepted overlap thresholds or painted-obstacle contract. Result: visibility tracking now marks geometry pending at setup and at relevant animation/transition start, refuses to evaluate while the content-flow or an ancestor has a running animation, and makes the rail reveal decision only in the completed measurement pass scheduled by motion end. Resize observation now requests measurement without independently declaring geometry ready. Human QA confirmed organic Home-to-post navigation now makes the correct initial decision; the temporary diagnostic UI and session state were then removed.
- P19. Let content blocks declare an explicit painted TOC obstacle so transparent wide-layout shells do not count as visual obstruction while undeclared blocks retain the conservative root fallback. Result: `ArticleToc` now prefers top-level `[data-toc-obstacle]` surfaces within each direct content child, reads an optional computed matte outset, and falls back to the direct child only when a block declares no surface. Image blocks declare the actual image, except full images whose matte-bearing figure remains the obstacle. Human QA approved the settled scroll-time behavior after the initial-load defect was isolated and fixed separately. P28 later removed the full-image exception after QA showed that its viewport-wide figure and button substantially exceeded the visible height-capped media.
- P20. Add the missing article-context underlap matte to wide/full core Gallery blocks and measure the same gallery footprint for TOC occlusion. Result: core Gallery now declares its root as the obstacle and applies the shared 25px matte/outset at wide and full alignment; nested Image markers are ignored because the Gallery root owns the footprint. Human QA confirmed the gallery footprint and TOC visibility now agree.
- P21. Align Embed matte painting and TOC measurement around the rendered embed frame rather than its potentially wider transparent figure shell. Result: Embed now wraps its frame in a declared `.embed-surface`; provider sizing, wide/full matte painting, and the 25px measured outset all use that same wrapper. Human QA approved the settled scroll-time behavior.
- P18. Correct the expanded-TOC geometry calculation so an already-expanded rail does not count its list height twice and dilute the initial obstruction ratio. Result: the list height is now added only while estimating a collapsed rail's expanded footprint; an open rail uses its actual rendered height. Human QA confirmed the corrected initial obstruction ratio after transition timing was fixed.
- P14. Prevent the desktop TOC from briefly appearing with stale initial geometry when a large image or other late-settling object already covers at least 40% of the expanded rail. Result: visibility tracking now reinitializes after the asynchronous heading scan renders the rail, keeps the rail hidden until its first observed geometry pass, and rechecks direct content blocks when their size or image-loading state changes. Initial human QA still exposed a visible rail because the expanded-height calculation double-counted the list; P18 corrects that separate geometry error. Final human QA confirmed the rail no longer appears incorrectly on fresh or client-side entry.
- P17. Move the desktop TOC another 25px left while retaining the viewport clamp and desktop cutoff behavior. Result: the desktop offset moved from 70px to 95px while preserving the existing `max(var(--space-5), …)` clamp. Human QA approved the final TOC placement as part of the completed behavior pass.
- P22. Add temporary opt-in TOC geometry instrumentation for the remaining initial-load failure before changing the heuristic again. Under `?toc-debug=1`, record each evaluation trigger, expanded rail rectangle, declared painted obstacle rectangles and matte outsets, overlap ratios, image readiness, and the resulting visibility decision; draw matching viewport overlays so the measured geometry can be compared directly with what is painted. Use the trace to distinguish a scheduling/readiness failure from an incorrect obstacle contract, then remove the instrumentation as part of the eventual fix. Result: `ArticleToc` now exposes an opt-in fixed diagnostic panel and viewport overlays, and emits each structured evaluation to the console without changing the production heuristic. Debug activation persists in the current browser tab after visiting any TOC route with `?toc-debug=1`, allowing the user to return Home and reproduce the failure through ordinary client-side navigation without a query parameter on the destination; `?toc-debug=0` clears it. Diagnostic result: organic Home-to-writing navigation measured the animated content at `top: 918.9px` and incorrectly classified the rail as outside the viewport, even though the final painted rail sat near `515px`. The later scroll trace measured the settled layout correctly, proving the remaining defect was transition timing rather than the 40% threshold or painted-obstacle geometry.
- P15. Make the Columns block's cream ground contextual so it remains available on TOC-bearing article flows but is transparent on Side Projects and in the WordPress editor. Result: Columns now reads the article-only matte color with a transparent fallback; regenerated editor CSS retains the grid without the cream ground. Human QA confirmed the unwanted cream ground is gone.
- P16. Make floated-image cream matting contextual so non-article content flows do not inherit an article-ground halo or its associated padding. Result: regular and resized float mat sizes plus the cream background are now supplied only by `has-underlap-matte`; Side Projects falls back to zero mat and transparent ground. Human QA confirmed the matte is gone with no regression.
- P4. Summarize the observed root cause and choose the smallest architectural boundary for the fix before editing `underlap-matte` or its callsites. Do not assume a global mixin rewrite is necessary until the reproduction matrix supports it. Result: source and generated-CSS inspection showed that an isolated stacking context paints the negative-z matte above its parent's background, border, and shadow; the fix therefore separates contextual matte activation from foreground block surfaces instead of globally replacing the mixin.
- P5. Implement the settled paint-order fix so File and Accordion backgrounds, borders, and hard shadows remain intact at default/wide/full alignments. Result: wide/full File and Accordion recipes now repaint their visual surfaces on a foreground `::after` layer above the matte while their content remains interactive above both layers.
- P6. Remove frontend TOC-clearing material from the WordPress editor without degrading normal editor block styling. Result: the editor configures the shared mixins with underlap emission disabled, the code-block matte falls back to transparent outside an opted-in article flow, and the regenerated editor stylesheet retains ordinary block recipes without cream matte pseudo-elements.
- P7. Remove or disable cream underlap clearing on Side Projects and settle whether non-cream contexts opt out or supply an appropriate contextual ground. Result: `BlockRenderer` now defaults to no matte; writing and case-study detail pages opt in explicitly, so Side Projects and other non-TOC flows remain transparent.
- P8. Audit remaining `underlap-matte` callsites for the same layering/context failure and make only evidence-backed follow-up changes. Result: remaining surfaced recipes already place their visible surface in a child or foreground layer, use the same cream as the matte, or compose the matte into their shadow; no additional block-specific changes were warranted.
- P9. Extend the desktop TOC auto-hide heuristic so either at least three meaningful intrusions or one sufficiently large obstruction can hide the expanded rail. Choose and document a conservative area/coverage threshold based on rendered geometry. Result: the rail now hides for the existing three qualifying overlaps or when one qualifying object covers at least 40% of the expanded TOC area.
- P10. Move the desktop TOC approximately 25px farther left, preserving its existing viewport clamp and verifying widths near the desktop cutoff. Result: the desktop offset changed from 45px to 70px while retaining the existing `max(var(--space-5), …)` viewport clamp; breakpoint-edge verification is queued for human QA.
- P11. Regenerate the WordPress editor stylesheet and run `corepack pnpm check`. Result: the generated stylesheet is current; check completed with no errors and only the two pre-existing `vue/no-v-html` warnings in `about.vue` and `now.vue`.
- P13. Fold any durable underlap/TOC context rule into `AGENTS.md`, `docs/design-system.md`, or `docs/visual-design.md` before archiving the spike. Result: `AGENTS.md` and `docs/design-system.md` now define matte activation as an explicit TOC-content context and require surfaced blocks to paint above it.
